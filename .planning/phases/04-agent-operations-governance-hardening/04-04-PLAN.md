---
phase: 04-agent-operations-governance-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - docs/runbooks/agent-maintenance-workflow.md
  - docs/runbooks/annuity-performance-replay.md
  - docs/runbooks/annual-award-replay.md
  - docs/runbooks/annual-loss-replay.md
  - tests/contracts/test_phase4_runbook_contracts.py
autonomous: true
requirements:
  - OPS-02
  - OPS-04
must_haves:
  truths:
    - "One canonical runbook shows how to add a source, adjust a rule, run verification, inspect lineage/evidence, and transition compatibility cases."
    - "Domain replay runbooks point to the same command and file-surface contract as the canonical workflow."
    - "Workflow documentation is protected by contract tests so it cannot drift silently from the implemented CLI and config layout."
  artifacts:
    - path: "docs/runbooks/agent-maintenance-workflow.md"
      provides: "Single canonical agent maintenance workflow."
      contains: "Add a source,Adjust a rule,Run verify,Inspect evidence,Compatibility case lifecycle"
    - path: "tests/contracts/test_phase4_runbook_contracts.py"
      provides: "Executable drift protection for workflow docs."
      contains: "test_agent_workflow_runbook_references_exact_commands,test_domain_runbooks_reference_lookup_and_compatibility_commands"
  key_links:
    - from: "docs/runbooks/agent-maintenance-workflow.md"
      to: "src/work_data_hub_pro/apps/etl_cli/main.py"
      via: "exact replay and compatibility command forms"
      pattern: "replay run|replay diagnose|replay lookup|compatibility show-case|compatibility transition-case|compatibility close-case"
    - from: "docs/runbooks/agent-maintenance-workflow.md"
      to: "src/work_data_hub_pro/apps/orchestration/replay/registry.py"
      via: "bounded file surfaces for source onboarding and runbook linkage"
      pattern: "runbook_path|release_path|domain_config_path"
---

<objective>
Document one bounded, executable maintenance workflow that ties the new Phase 4 code contracts together.

Purpose: satisfy OPS-02 and close OPS-04 at the human/agent handoff layer by freezing one repo-native workflow for add-source / adjust-rule / run-verify / inspect-evidence behavior.
Output: a canonical workflow runbook, aligned domain runbooks, and contract tests that fail when docs drift from the real code/config/CLI surfaces.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agent-operations-governance-hardening/04-CONTEXT.md
@.planning/phases/04-agent-operations-governance-hardening/04-RESEARCH.md
@docs/runbooks/annuity-performance-replay.md
@docs/runbooks/annual-award-replay.md
@docs/runbooks/annual-loss-replay.md
@src/work_data_hub_pro/apps/etl_cli/main.py
@src/work_data_hub_pro/apps/orchestration/replay/registry.py
@config/domains/
@config/releases/

<interfaces>
By the time this plan starts, the repo should expose:
- `replay run`
- `replay diagnose`
- `replay lookup`
- `compatibility show-case`
- `compatibility transition-case`

The canonical source-onboarding and rule-adjustment surfaces are:
- replay registry metadata
- `config/domains/<domain>/`
- `config/releases/`
- replay assets under `reference/historical_replays/<domain>/`
- contract/integration test commands under `uv run pytest`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical agent maintenance workflow runbook with exact repo surfaces and command forms</name>
  <files>docs/runbooks/agent-maintenance-workflow.md</files>
  <read_first>
    .planning/phases/04-agent-operations-governance-hardening/04-CONTEXT.md
    .planning/phases/04-agent-operations-governance-hardening/04-RESEARCH.md
    src/work_data_hub_pro/apps/etl_cli/main.py
    src/work_data_hub_pro/apps/orchestration/replay/registry.py
    docs/runbooks/annuity-performance-replay.md
  </read_first>
  <action>Create `docs/runbooks/agent-maintenance-workflow.md` with the exact sections `Add a source`, `Adjust a rule`, `Run verify`, `Inspect evidence`, and `Compatibility case lifecycle`. In `Add a source`, name the exact repo surfaces `src/work_data_hub_pro/apps/orchestration/replay/registry.py`, `config/domains/<domain>/`, `config/releases/`, `reference/historical_replays/<domain>/`, and `docs/runbooks/<domain>-replay.md`. In `Adjust a rule`, name `config/domains/<domain>/cleansing.json` and the relevant release binding file. In `Run verify`, include the exact command shapes `uv run python -m work_data_hub_pro.apps.etl_cli.main replay run --domain <domain> --workbook <path> --period <period>`, `uv run python -m work_data_hub_pro.apps.etl_cli.main replay diagnose --comparison-run-id <id>`, `uv run python -m work_data_hub_pro.apps.etl_cli.main replay lookup --comparison-run-id <id> --record-id <id>`, and `uv run pytest -v`. In `Compatibility case lifecycle`, include `compatibility show-case`, `compatibility transition-case`, and `compatibility close-case` with `--evidence-root`, `--case-id`, `--status` where applicable, `--owner`, `--resolution-note`, and `--closure-evidence` for the close flow.</action>
  <acceptance_criteria>`rg -n "Add a source|Adjust a rule|Run verify|Inspect evidence|Compatibility case lifecycle|replay run --domain|replay diagnose --comparison-run-id|replay lookup --comparison-run-id|compatibility show-case|compatibility transition-case|compatibility close-case|config/domains/<domain>/cleansing.json|config/releases/" docs/runbooks/agent-maintenance-workflow.md` returns hits for every required section and command string.</acceptance_criteria>
  <verify>
    <automated>rg -n "Add a source|Adjust a rule|Run verify|Inspect evidence|Compatibility case lifecycle|replay run --domain|replay diagnose --comparison-run-id|replay lookup --comparison-run-id|compatibility show-case|compatibility transition-case|compatibility close-case|config/domains/<domain>/cleansing.json|config/releases/" docs/runbooks/agent-maintenance-workflow.md</automated>
  </verify>
  <done>The repo has one canonical workflow document that shows the exact bounded change surface and evidence/verification commands an agent must use.</done>
</task>

<task type="auto">
  <name>Task 2: Align the domain replay runbooks to the canonical maintenance workflow</name>
  <files>docs/runbooks/annuity-performance-replay.md, docs/runbooks/annual-award-replay.md, docs/runbooks/annual-loss-replay.md</files>
  <read_first>
    docs/runbooks/agent-maintenance-workflow.md
    docs/runbooks/annuity-performance-replay.md
    docs/runbooks/annual-award-replay.md
    docs/runbooks/annual-loss-replay.md
  </read_first>
  <action>Update all three domain replay runbooks so each one references the new canonical workflow and includes the exact command forms for `replay run`, `replay diagnose`, `replay lookup`, and compatibility case handling. Add one short section in each runbook naming the canonical workflow file `docs/runbooks/agent-maintenance-workflow.md`. Do not duplicate the full workflow in each domain file; keep the domain runbooks focused on domain-specific workbook and replay-root details while pointing back to the canonical flow for source onboarding, rule adjustment, and case handling.</action>
  <acceptance_criteria>`rg -n "agent-maintenance-workflow.md|replay lookup --comparison-run-id|replay diagnose --comparison-run-id|replay run --domain|compatibility show-case|compatibility close-case" docs/runbooks/annuity-performance-replay.md docs/runbooks/annual-award-replay.md docs/runbooks/annual-loss-replay.md` returns hits in all three runbooks.</acceptance_criteria>
  <verify>
    <automated>rg -n "agent-maintenance-workflow.md|replay lookup --comparison-run-id|replay diagnose --comparison-run-id|replay run --domain|compatibility show-case|compatibility close-case" docs/runbooks/annuity-performance-replay.md docs/runbooks/annual-award-replay.md docs/runbooks/annual-loss-replay.md</automated>
  </verify>
  <done>Domain-specific runbooks now align to the same canonical maintenance contract instead of drifting into different operational stories.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract tests that fail when workflow documentation drifts from the implemented command and config surface</name>
  <files>tests/contracts/test_phase4_runbook_contracts.py</files>
  <read_first>
    docs/runbooks/agent-maintenance-workflow.md
    docs/runbooks/annuity-performance-replay.md
    docs/runbooks/annual-award-replay.md
    docs/runbooks/annual-loss-replay.md
    src/work_data_hub_pro/apps/etl_cli/main.py
    src/work_data_hub_pro/apps/orchestration/replay/registry.py
  </read_first>
  <action>Create `tests/contracts/test_phase4_runbook_contracts.py` with one test that asserts `docs/runbooks/agent-maintenance-workflow.md` contains the exact CLI strings `replay run --domain`, `replay diagnose --comparison-run-id`, `replay lookup --comparison-run-id`, `compatibility show-case`, `compatibility transition-case`, and `compatibility close-case`, plus the exact repo path strings `src/work_data_hub_pro/apps/orchestration/replay/registry.py`, `config/domains/`, `config/releases/`, and `reference/historical_replays/`. Add a second test that asserts each domain runbook mentions `agent-maintenance-workflow.md`, `replay diagnose`, `replay lookup`, and compatibility-case handling. Keep the tests file-based and deterministic; do not attempt to execute the full human workflow here.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_phase4_runbook_contracts.py -v` passes. `rg -n "agent-maintenance-workflow.md|replay run --domain|replay diagnose --comparison-run-id|replay lookup --comparison-run-id|compatibility show-case|compatibility transition-case|compatibility close-case|src/work_data_hub_pro/apps/orchestration/replay/registry.py|config/domains/|config/releases/|reference/historical_replays/" tests/contracts/test_phase4_runbook_contracts.py` returns hits for every required string.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_phase4_runbook_contracts.py -v</automated>
    <automated>rg -n "agent-maintenance-workflow.md|replay run --domain|replay diagnose --comparison-run-id|replay lookup --comparison-run-id|compatibility show-case|compatibility transition-case|compatibility close-case|src/work_data_hub_pro/apps/orchestration/replay/registry.py|config/domains/|config/releases/|reference/historical_replays/" tests/contracts/test_phase4_runbook_contracts.py</automated>
  </verify>
  <done>The maintenance workflow promise is now protected by executable contract checks instead of informal documentation alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow documentation -> operator execution | If docs drift from real commands or paths, the phase fails OPS-02 even when code exists. |
| canonical workflow -> domain runbooks | Per-domain runbooks must not diverge into incompatible maintenance stories. |
| doc claims -> actual repo surfaces | Runbooks must describe only committed, executable repo surfaces, not hidden local practices. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | R | maintenance workflow docs | mitigate | Freeze exact command strings and repo paths in `tests/contracts/test_phase4_runbook_contracts.py`. |
| T-04-11 | T | domain runbook drift | mitigate | Require all three domain runbooks to reference the canonical workflow and the same lookup/diagnose command forms. |
| T-04-12 | I | hidden local-only operator knowledge | mitigate | Limit the workflow to committed code/config/runbook/test surfaces and explicitly name every required path. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_phase4_runbook_contracts.py -v`
</verification>

<success_criteria>
- One canonical runbook defines the bounded add-source / adjust-rule / run-verify / inspect-evidence workflow.
- All three domain runbooks reference the same lookup and compatibility command surfaces.
- Contract tests fail when workflow docs drift from the implemented repo surface.
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-operations-governance-hardening/04-04-SUMMARY.md`
</output>
