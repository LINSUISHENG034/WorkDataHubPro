---
phase: 04-agent-operations-governance-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - docs/runbooks/agent-maintenance-workflow.md
  - docs/runbooks/annuity-performance-replay.md
  - docs/runbooks/annual-award-replay.md
  - docs/runbooks/annual-loss-replay.md
  - docs/runbooks/annuity-income-replay.md
  - tests/contracts/test_phase4_runbook_contracts.py
autonomous: true
requirements:
  - OPS-02
  - OPS-04
must_haves:
  truths:
    - "One canonical runbook shows the exact bounded maintenance workflow supported by the implemented Phase 4 CLI and evidence contracts."
    - "All currently registered replay-domain runbooks point to the same canonical workflow and the same lookup and compatibility command surfaces."
    - "Workflow documentation is protected by contract tests that check registry path existence and CLI help surfaces, not only raw string presence."
  artifacts:
    - path: "docs/runbooks/agent-maintenance-workflow.md"
      provides: "Single canonical agent maintenance workflow with bounded scope."
      contains: "Current bounded scope: annuity_performance, annual_award, annual_loss, annuity_income"
    - path: "tests/contracts/test_phase4_runbook_contracts.py"
      provides: "Executable drift protection for workflow docs, registry paths, and CLI surfaces."
      contains: "test_registry_specs_reference_existing_paths,test_cli_help_exposes_documented_lookup_and_compatibility_commands,test_registered_domain_runbooks_reference_canonical_workflow"
  key_links:
    - from: "docs/runbooks/agent-maintenance-workflow.md"
      to: "src/work_data_hub_pro/apps/etl_cli/main.py"
      via: "exact replay and compatibility command forms implemented by Plans 01-03"
      pattern: "replay run|replay diagnose|replay lookup|compatibility show-case|compatibility transition-case|compatibility close-case"
    - from: "tests/contracts/test_phase4_runbook_contracts.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/registry.py"
      via: "registry-backed contract tests verify path existence for all registered domains"
      pattern: "runbook_path|release_path|domain_config_path"
    - from: "docs/runbooks/annuity-performance-replay.md"
      to: "docs/runbooks/agent-maintenance-workflow.md"
      via: "domain runbooks delegate to the canonical workflow instead of duplicating it"
      pattern: "agent-maintenance-workflow.md"
---

<objective>
Document one bounded, executable maintenance workflow that matches exactly what Plans 04-01 through 04-03 implement.

Purpose: satisfy OPS-02 and close OPS-04 at the human/agent handoff layer by freezing one repo-native workflow for add-source / adjust-rule / run-verify / inspect-evidence behavior without overstating unsupported capabilities.
Output: a canonical workflow runbook, aligned runbooks for all currently registered replay domains, and contract tests that fail when docs drift from the real registry, path, or CLI surfaces.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agent-operations-governance-hardening/04-CONTEXT.md
@.planning/phases/04-agent-operations-governance-hardening/04-RESEARCH.md
@.planning/phases/04-agent-operations-governance-hardening/04-REVIEWS.md
@.planning/phases/04-agent-operations-governance-hardening/04-VALIDATION.md
@docs/runbooks/annuity-performance-replay.md
@docs/runbooks/annual-award-replay.md
@docs/runbooks/annual-loss-replay.md
@docs/runbooks/annuity-income-replay.md
@src/work_data_hub_pro/apps/etl_cli/main.py
@src/work_data_hub_pro/apps/orchestration/replay/registry.py
@config/domains/
@config/releases/

<interfaces>
By the time this plan starts, the repo should expose:
- `replay run`
- `replay diagnose`
- `replay lookup`
- `compatibility show-case`
- `compatibility transition-case`
- `compatibility close-case`

Current registered replay domains are:
- `annuity_performance`
- `annual_award`
- `annual_loss`
- `annuity_income`

The bounded source-onboarding and rule-adjustment surfaces are:
- replay registry metadata in `src/work_data_hub_pro/apps/orchestration/replay/registry.py`
- `config/domains/<domain>/`
- `config/releases/`
- replay assets under `reference/historical_replays/<domain>/`
- runbooks under `docs/runbooks/`
- verification commands under `uv run pytest`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical agent maintenance workflow runbook with exact scope bounds and command forms</name>
  <files>docs/runbooks/agent-maintenance-workflow.md</files>
  <read_first>
    .planning/phases/04-agent-operations-governance-hardening/04-CONTEXT.md
    .planning/phases/04-agent-operations-governance-hardening/04-REVIEWS.md
    .planning/phases/04-agent-operations-governance-hardening/04-VALIDATION.md
    src/work_data_hub_pro/apps/etl_cli/main.py
    src/work_data_hub_pro/apps/orchestration/replay/registry.py
    docs/runbooks/annuity-performance-replay.md
    docs/runbooks/annuity-income-replay.md
  </read_first>
  <action>Per D-01, D-02, and D-03, and addressing the 04-04 review concern that documentation must stay bounded to implemented behavior, create `docs/runbooks/agent-maintenance-workflow.md` with the exact sections `Current bounded scope`, `Add a source`, `Adjust a rule`, `Run verify`, `Inspect evidence`, `Compatibility case lifecycle`, and `Manual verification checklist`. In `Current bounded scope`, include the exact sentence `Current bounded scope: annuity_performance, annual_award, annual_loss, annuity_income.` followed by `Adding a brand-new domain is not a doc-only change; it requires updates to src/work_data_hub_pro/apps/orchestration/replay/registry.py, config/domains/<domain>/, config/releases/, reference/historical_replays/<domain>/, and docs/runbooks/<domain>-replay.md.` In `Run verify`, include the exact command shapes `uv run python -m work_data_hub_pro.apps.etl_cli.main replay run --domain <domain> --workbook <path> --period <period>`, `uv run python -m work_data_hub_pro.apps.etl_cli.main replay diagnose --comparison-run-id <id>`, `uv run python -m work_data_hub_pro.apps.etl_cli.main replay lookup --comparison-run-id <id> --record-id <id>`, and `uv run pytest -v`. In `Compatibility case lifecycle`, include `compatibility show-case`, `compatibility transition-case`, and `compatibility close-case` with `--evidence-root`, `--case-id`, `--status` where applicable, `--owner`, `--resolution-note`, and `--closure-evidence` for the close flow. In `Manual verification checklist`, copy the exact Phase 4 validation idea from `04-VALIDATION.md`: inspect registry/config files, run `replay run`, run `replay diagnose`, run the lookup command, and confirm the compatibility-case flow matches the implemented CLI.</action>
  <acceptance_criteria>`rg -n "Current bounded scope: annuity_performance, annual_award, annual_loss, annuity_income|Adding a brand-new domain is not a doc-only change|Add a source|Adjust a rule|Run verify|Inspect evidence|Compatibility case lifecycle|Manual verification checklist|replay run --domain|replay diagnose --comparison-run-id|replay lookup --comparison-run-id|compatibility show-case|compatibility transition-case|compatibility close-case" docs/runbooks/agent-maintenance-workflow.md` returns hits for every required scope statement, section, and command string.</acceptance_criteria>
  <verify>
    <automated>rg -n "Current bounded scope: annuity_performance, annual_award, annual_loss, annuity_income|Adding a brand-new domain is not a doc-only change|Add a source|Adjust a rule|Run verify|Inspect evidence|Compatibility case lifecycle|Manual verification checklist|replay run --domain|replay diagnose --comparison-run-id|replay lookup --comparison-run-id|compatibility show-case|compatibility transition-case|compatibility close-case" docs/runbooks/agent-maintenance-workflow.md</automated>
  </verify>
  <done>The repo has one canonical workflow document that names the exact supported scope, change surface, evidence flow, and verification commands an agent must use.</done>
</task>

<task type="auto">
  <name>Task 2: Align all registered domain runbooks to the canonical workflow without duplicating unsupported claims</name>
  <files>docs/runbooks/annuity-performance-replay.md, docs/runbooks/annual-award-replay.md, docs/runbooks/annual-loss-replay.md, docs/runbooks/annuity-income-replay.md</files>
  <read_first>
    docs/runbooks/agent-maintenance-workflow.md
    docs/runbooks/annuity-performance-replay.md
    docs/runbooks/annual-award-replay.md
    docs/runbooks/annual-loss-replay.md
    docs/runbooks/annuity-income-replay.md
    src/work_data_hub_pro/apps/orchestration/replay/registry.py
  </read_first>
  <action>Update all four registered domain replay runbooks so each one references the canonical workflow file `docs/runbooks/agent-maintenance-workflow.md` and includes the exact command forms for `replay run`, `replay diagnose`, `replay lookup`, and compatibility case handling that Plans 04-01 through 04-03 actually implement. Add one short section in each runbook naming the canonical workflow file and stating that domain-specific workbook/replay-root details stay local to that runbook. Do not claim dashboard workflows, persistent storage backends, or manual JSON editing; keep each runbook focused on its current registry-backed domain and point back to the canonical workflow for source onboarding, rule adjustment, and case handling.</action>
  <acceptance_criteria>`rg -n "agent-maintenance-workflow.md|replay lookup --comparison-run-id|replay diagnose --comparison-run-id|replay run --domain|compatibility show-case|compatibility close-case" docs/runbooks/annuity-performance-replay.md docs/runbooks/annual-award-replay.md docs/runbooks/annual-loss-replay.md docs/runbooks/annuity-income-replay.md` returns hits in all four registered domain runbooks.</acceptance_criteria>
  <verify>
    <automated>rg -n "agent-maintenance-workflow.md|replay lookup --comparison-run-id|replay diagnose --comparison-run-id|replay run --domain|compatibility show-case|compatibility close-case" docs/runbooks/annuity-performance-replay.md docs/runbooks/annual-award-replay.md docs/runbooks/annual-loss-replay.md docs/runbooks/annuity-income-replay.md</automated>
  </verify>
  <done>Every registered replay-domain runbook now delegates to the same canonical maintenance contract instead of drifting into incompatible operational stories.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract tests that verify registry paths and CLI help surfaces, not only doc strings</name>
  <files>tests/contracts/test_phase4_runbook_contracts.py</files>
  <read_first>
    docs/runbooks/agent-maintenance-workflow.md
    docs/runbooks/annuity-performance-replay.md
    docs/runbooks/annual-award-replay.md
    docs/runbooks/annual-loss-replay.md
    docs/runbooks/annuity-income-replay.md
    src/work_data_hub_pro/apps/etl_cli/main.py
    src/work_data_hub_pro/apps/orchestration/replay/registry.py
  </read_first>
  <action>Addressing the 04-04 review concern about stronger contract tests, create `tests/contracts/test_phase4_runbook_contracts.py` with the exact tests `test_registry_specs_reference_existing_paths`, `test_cli_help_exposes_documented_lookup_and_compatibility_commands`, `test_agent_workflow_runbook_references_exact_commands`, and `test_registered_domain_runbooks_reference_canonical_workflow`. `test_registry_specs_reference_existing_paths` must import the replay registry and assert every registered domain spec has existing `runbook_path`, `release_path`, and `domain_config_path` files on disk. `test_cli_help_exposes_documented_lookup_and_compatibility_commands` must use the Typer app help output or `CliRunner` to assert `replay lookup`, `compatibility show-case`, `compatibility transition-case`, and `compatibility close-case` exist. `test_agent_workflow_runbook_references_exact_commands` must assert the canonical runbook contains the exact command and repo-path strings documented in Task 1. `test_registered_domain_runbooks_reference_canonical_workflow` must assert each registered domain runbook mentions `agent-maintenance-workflow.md`, `replay diagnose`, `replay lookup`, and compatibility-case handling.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_phase4_runbook_contracts.py -v` passes. `rg -n "test_registry_specs_reference_existing_paths|test_cli_help_exposes_documented_lookup_and_compatibility_commands|test_agent_workflow_runbook_references_exact_commands|test_registered_domain_runbooks_reference_canonical_workflow|runbook_path|release_path|domain_config_path|replay lookup|compatibility show-case|compatibility transition-case|compatibility close-case" tests/contracts/test_phase4_runbook_contracts.py` returns hits for every required test and validated surface.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_phase4_runbook_contracts.py -v</automated>
    <automated>rg -n "test_registry_specs_reference_existing_paths|test_cli_help_exposes_documented_lookup_and_compatibility_commands|test_agent_workflow_runbook_references_exact_commands|test_registered_domain_runbooks_reference_canonical_workflow|runbook_path|release_path|domain_config_path|replay lookup|compatibility show-case|compatibility transition-case|compatibility close-case" tests/contracts/test_phase4_runbook_contracts.py</automated>
  </verify>
  <done>The maintenance workflow promise is protected by executable checks against the registry, CLI surface, and runbook content instead of informal documentation alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow documentation -> operator execution | If docs drift from real commands or paths, the phase fails OPS-02 even when code exists. |
| canonical workflow -> registered domain runbooks | Per-domain runbooks must not diverge into incompatible maintenance stories. |
| doc claims -> actual repo surfaces | Runbooks must describe only committed, executable repo surfaces, not hidden local practices or unimplemented future work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | R | maintenance workflow docs | mitigate | Freeze exact command strings, bounded-scope statements, and repo paths in the canonical runbook contract test. |
| T-04-12 | T | registered domain runbook drift | mitigate | Require all four registered domain runbooks to reference the canonical workflow and the same lookup and compatibility command forms. |
| T-04-13 | I | hidden local-only operator knowledge | mitigate | Validate registry path existence and CLI help surfaces directly so the docs describe only implemented repo-native behavior. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_phase4_runbook_contracts.py -v`
</verification>

<success_criteria>
- One canonical runbook defines the bounded add-source / adjust-rule / run-verify / inspect-evidence workflow for the currently registered domains.
- All four registered domain runbooks reference the same lookup and compatibility command surfaces.
- Contract tests fail when workflow docs drift from implemented registry paths or CLI surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-operations-governance-hardening/04-04-SUMMARY.md`
</output>
