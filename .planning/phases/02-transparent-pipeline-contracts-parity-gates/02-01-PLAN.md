---
phase: 02-transparent-pipeline-contracts-parity-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/work_data_hub_pro/platform/contracts/models.py
  - src/work_data_hub_pro/governance/compatibility/models.py
  - src/work_data_hub_pro/governance/compatibility/gate_models.py
  - src/work_data_hub_pro/governance/compatibility/gate_runtime.py
  - src/work_data_hub_pro/governance/adjudication/service.py
  - src/work_data_hub_pro/governance/evidence_index/file_store.py
  - tests/contracts/test_system_contracts.py
  - tests/contracts/test_phase2_gate_contracts.py
  - tests/integration/test_compatibility_adjudication.py
autonomous: true
requirements:
  - PIPE-01
  - PAR-03
must_haves:
  truths:
    - "Replay checkpoints and comparison-run artifacts are explicit contracts rather than slice-local conventions."
    - "Shared gate execution and failed-package writing live in a reusable runtime module instead of slice-local orchestration code."
    - "Compatibility adjudication distinguishes block versus warn, precedent versus exception, and checkpoint identity."
    - "Failed-gate evidence can be serialized through stable file/package primitives before any slice wiring begins."
  artifacts:
    - path: "src/work_data_hub_pro/governance/compatibility/gate_models.py"
      provides: "Shared checkpoint, gate summary, and comparison-run manifest contracts."
      contains: "CheckpointResult, GateSummary, ComparisonRunManifest"
    - path: "src/work_data_hub_pro/governance/compatibility/models.py"
      provides: "Expanded compatibility-case semantics for Phase 2 adjudication."
      contains: "severity, decision_status, precedent_status, precedent_key, expires_at, checkpoint_name, comparison_run_id"
    - path: "src/work_data_hub_pro/governance/compatibility/gate_runtime.py"
      provides: "Reusable checkpoint evaluation, gate summary, and failed-package writing helpers."
      contains: "build_checkpoint_result,summarize_gate_results,write_comparison_run_package"
    - path: "tests/contracts/test_phase2_gate_contracts.py"
      provides: "Machine checks for gate/evidence contract shape."
      contains: "test_checkpoint_result_fields,test_manifest_package_paths,test_compatibility_case_phase2_fields"
  key_links:
    - from: "src/work_data_hub_pro/governance/compatibility/gate_runtime.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py"
      via: "shared checkpoint evaluation and package writing entrypoints"
      pattern: "build_checkpoint_result|summarize_gate_results|write_comparison_run_package"
    - from: "src/work_data_hub_pro/governance/compatibility/gate_models.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py"
      via: "shared checkpoint result and summary types"
      pattern: "CheckpointResult|GateSummary"
    - from: "src/work_data_hub_pro/governance/evidence_index/file_store.py"
      to: ".planning/phases/02-transparent-pipeline-contracts-parity-gates/02-CONTEXT.md"
      via: "failed-gate evidence package filenames"
      pattern: "manifest.json|gate-summary.json|checkpoint-results.json|publication-results.json"
---

<objective>
Create the shared contract, runtime, and evidence foundation for Phase 2 so later plans can wire deterministic parity gates without inventing slice-specific result structures.

Purpose: satisfy the contract and adjudication core of `PIPE-01` and `PAR-03` before any replay-slice wiring.
Output: explicit gate models, reusable gate-runtime helpers, richer compatibility-case semantics, evidence-package writer support, and contract tests.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-CONTEXT.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-RESEARCH.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-VALIDATION.md
@docs/gsd/reviews/2026-04-12-phase2-gray-area-review.md
@docs/superpowers/specs/2026-04-11-workdatahubpro-rebuild-architecture-draft.md

<interfaces>
From `src/work_data_hub_pro/platform/contracts/models.py`:
- `InputBatch`
- `InputRecord`
- `CanonicalFactRecord`
- `FieldTraceEvent`

From `src/work_data_hub_pro/governance/compatibility/models.py`:
- `CompatibilityCase`

From `src/work_data_hub_pro/governance/evidence_index/file_store.py`:
- trace storage currently roots evidence under `trace/`
- compatibility-case storage currently roots evidence under `compatibility_cases/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define explicit checkpoint contracts and shared gate-runtime entrypoints</name>
  <files>src/work_data_hub_pro/governance/compatibility/gate_models.py, src/work_data_hub_pro/governance/compatibility/gate_runtime.py, src/work_data_hub_pro/platform/contracts/models.py</files>
  <read_first>
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-CONTEXT.md
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-RESEARCH.md
    src/work_data_hub_pro/platform/contracts/models.py
    docs/gsd/reviews/2026-04-12-phase2-gray-area-review.md
  </read_first>
  <action>Create `src/work_data_hub_pro/governance/compatibility/gate_models.py` with frozen dataclasses named `CheckpointFingerprint`, `CheckpointDiff`, `CheckpointResult`, `GateSummary`, and `ComparisonRunManifest`. `CheckpointResult` must include `comparison_run_id`, `checkpoint_name`, `checkpoint_type`, `status`, `severity`, `legacy_fingerprint`, `pro_fingerprint`, `diff_path`, and `trace_anchor_rows`. `GateSummary` must include overall outcome plus per-severity counts. Create `src/work_data_hub_pro/governance/compatibility/gate_runtime.py` with orchestration-agnostic helpers named `build_checkpoint_result`, `summarize_gate_results`, and `write_comparison_run_package` so later slice plans do not serialize gate output inline. Update `src/work_data_hub_pro/platform/contracts/models.py` only where shared type aliases or imports are needed to keep checkpoint contracts discoverable beside existing stage contracts.</action>
  <acceptance_criteria>Run `rg -n "class CheckpointFingerprint|class CheckpointDiff|class CheckpointResult|class GateSummary|class ComparisonRunManifest|build_checkpoint_result|summarize_gate_results|write_comparison_run_package|comparison_run_id|checkpoint_name|trace_anchor_rows" src/work_data_hub_pro/governance/compatibility/gate_models.py src/work_data_hub_pro/governance/compatibility/gate_runtime.py src/work_data_hub_pro/platform/contracts/models.py` and confirm all class names, helper entrypoints, and required fields are present.</acceptance_criteria>
  <verify>
    <automated>rg -n "class CheckpointFingerprint|class CheckpointDiff|class CheckpointResult|class GateSummary|class ComparisonRunManifest|build_checkpoint_result|summarize_gate_results|write_comparison_run_package|comparison_run_id|checkpoint_name|trace_anchor_rows" src/work_data_hub_pro/governance/compatibility/gate_models.py src/work_data_hub_pro/governance/compatibility/gate_runtime.py src/work_data_hub_pro/platform/contracts/models.py</automated>
  </verify>
  <done>Shared checkpoint contracts and reusable gate-runtime entrypoints exist and match the Phase 2 decision baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Expand compatibility-case and adjudication semantics</name>
  <files>src/work_data_hub_pro/governance/compatibility/models.py, src/work_data_hub_pro/governance/adjudication/service.py</files>
  <read_first>
    src/work_data_hub_pro/governance/compatibility/models.py
    src/work_data_hub_pro/governance/adjudication/service.py
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-CONTEXT.md
    docs/gsd/reviews/2026-04-12-phase2-gray-area-review.md
  </read_first>
  <action>Update `CompatibilityCase` to include the exact Phase 2 fields `severity`, `decision_status`, `precedent_status`, `precedent_key`, `expires_at`, `checkpoint_name`, and `comparison_run_id` while preserving `case_id`, `sample_locator`, `legacy_result`, `pro_result`, `business_rationale`, `approved_by`, `affected_rule_version`, and `involved_anchor_row_nos`. Update `AdjudicationService.create_case(...)` to accept those new values or deterministic defaults: `severity="block"`, `decision_status="pending_review"`, `precedent_status="none"`, `precedent_key=None`, `expires_at=None`, and explicit caller-supplied `checkpoint_name` and `comparison_run_id`.</action>
  <acceptance_criteria>Run `rg -n "severity|decision_status|precedent_status|precedent_key|expires_at|checkpoint_name|comparison_run_id|pending_review|none" src/work_data_hub_pro/governance/compatibility/models.py src/work_data_hub_pro/governance/adjudication/service.py` and confirm every new field and default semantic appears in code.</acceptance_criteria>
  <verify>
    <automated>rg -n "severity|decision_status|precedent_status|precedent_key|expires_at|checkpoint_name|comparison_run_id|pending_review|none" src/work_data_hub_pro/governance/compatibility/models.py src/work_data_hub_pro/governance/adjudication/service.py</automated>
  </verify>
  <done>Compatibility-case semantics support Phase 2 block/warn, precedent, exception, and checkpoint identity requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Extend evidence indexing for comparison-run packages</name>
  <files>src/work_data_hub_pro/governance/evidence_index/file_store.py</files>
  <read_first>
    src/work_data_hub_pro/governance/evidence_index/file_store.py
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-CONTEXT.md
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-RESEARCH.md
  </read_first>
  <action>Add package-oriented writer methods to `FileEvidenceIndex` for the exact Phase 2 filenames `manifest.json`, `gate-summary.json`, `checkpoint-results.json`, `source-intake-adaptation.json`, `lineage-impact.json`, `publication-results.json`, `compatibility-case.json`, `report.md`, plus per-checkpoint files under `diffs/`. Organize them under a deterministic directory rooted by `comparison_run_id`, while preserving the existing `trace/` and `compatibility_cases/` write paths for anchor-row explainability and direct case lookup.</action>
  <acceptance_criteria>Run `rg -n "manifest.json|gate-summary.json|checkpoint-results.json|source-intake-adaptation.json|lineage-impact.json|publication-results.json|compatibility-case.json|report.md|diffs" src/work_data_hub_pro/governance/evidence_index/file_store.py` and confirm every required Phase 2 package path is represented.</acceptance_criteria>
  <verify>
    <automated>rg -n "manifest.json|gate-summary.json|checkpoint-results.json|source-intake-adaptation.json|lineage-impact.json|publication-results.json|compatibility-case.json|report.md|diffs" src/work_data_hub_pro/governance/evidence_index/file_store.py</automated>
  </verify>
  <done>File evidence indexing can persist the complete Phase 2 failed-gate package without replacing existing trace storage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add contract and integration tests for the foundation layer</name>
  <files>tests/contracts/test_system_contracts.py, tests/contracts/test_phase2_gate_contracts.py, tests/integration/test_compatibility_adjudication.py</files>
  <read_first>
    src/work_data_hub_pro/governance/compatibility/gate_models.py
    src/work_data_hub_pro/governance/compatibility/models.py
    src/work_data_hub_pro/governance/adjudication/service.py
    src/work_data_hub_pro/governance/evidence_index/file_store.py
    tests/contracts/test_system_contracts.py
    tests/integration/test_compatibility_adjudication.py
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `CheckpointResult`, `GateSummary`, and `ComparisonRunManifest` serialize required fields.
    - Test 2: expanded `CompatibilityCase` persists `severity`, `decision_status`, `precedent_status`, `checkpoint_name`, and `comparison_run_id`.
    - Test 3: `FileEvidenceIndex` writes the required Phase 2 package filenames under a deterministic comparison-run root.
  </behavior>
  <action>Create `tests/contracts/test_phase2_gate_contracts.py` for the new checkpoint/evidence models, extend `tests/contracts/test_system_contracts.py` only where existing core-contract coverage needs updated assertions, and extend `tests/integration/test_compatibility_adjudication.py` so persisted cases and file outputs assert the new Phase 2 fields and package filenames.</action>
  <acceptance_criteria>Run `uv run pytest tests/contracts/test_system_contracts.py tests/contracts/test_phase2_gate_contracts.py tests/integration/test_compatibility_adjudication.py -v` and confirm all tests pass.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_system_contracts.py tests/contracts/test_phase2_gate_contracts.py tests/integration/test_compatibility_adjudication.py -v</automated>
  </verify>
  <done>Phase 2 foundation contracts and adjudication semantics are machine-validated before slice wiring starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replay comparison results -> compatibility case | Weak semantics could misclassify blocking differences as benign or vice versa. |
| comparison-run evidence writer -> failed-gate package | Missing or unstable filenames could make gate failures non-diagnosable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `CompatibilityCase` semantics | mitigate | Add explicit `severity`, `decision_status`, `precedent_status`, `precedent_key`, `checkpoint_name`, and `comparison_run_id` fields with tests. |
| T-02-02 | R | comparison-run evidence package | mitigate | Enforce deterministic package filenames and root directory structure in contract tests. |
| T-02-03 | D | failed-gate diagnostics | mitigate | Preserve `trace/` storage while adding comparison-run package writers so row-level explainability remains available. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_system_contracts.py tests/contracts/test_phase2_gate_contracts.py tests/integration/test_compatibility_adjudication.py -v`
</verification>

<success_criteria>
- Shared checkpoint and gate-summary contracts exist and are test-covered.
- `CompatibilityCase` semantics match the Phase 2 decision baseline.
- File evidence indexing can emit all required failed-gate package filenames.
</success_criteria>

<output>
After completion, create `.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-01-SUMMARY.md`
</output>
