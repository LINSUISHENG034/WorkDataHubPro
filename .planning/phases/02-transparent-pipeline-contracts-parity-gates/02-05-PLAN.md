---
phase: 02-transparent-pipeline-contracts-parity-gates
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - config/verification/phase2-parity-gates.json
  - scripts/run_phase2_parity_gates.py
  - docs/runbooks/phase2-parity-gates.md
  - tests/contracts/test_phase2_ci_gate_matrix.py
autonomous: true
requirements:
  - PAR-04
must_haves:
  truths:
    - "Phase 2 has explicit PR, protected-branch, and nightly gate tiers that a CI system can invoke without guessing commands."
    - "The gate tiers reflect the completed Phase 2 scope: annuity-performance plus explainability retrieval on PRs, all accepted replay slices with `reference_derivation` closure on protected branches, and the full suite on nightly or release."
    - "The verification surface is repository-native and test-covered even though the repo does not currently ship a concrete CI provider configuration."
  artifacts:
    - path: "config/verification/phase2-parity-gates.json"
      provides: "Machine-readable CI tier to command mapping."
      contains: "pr,protected_branch,nightly"
    - path: "scripts/run_phase2_parity_gates.py"
      provides: "Executable tier runner for CI or local verification."
      contains: "--tier pr,--tier protected_branch,--tier nightly"
    - path: "tests/contracts/test_phase2_ci_gate_matrix.py"
      provides: "Contract checks that required tiers and commands exist."
      contains: "test_phase2_gate_matrix_has_three_tiers,test_phase2_gate_runner_supports_all_tiers"
  key_links:
    - from: "config/verification/phase2-parity-gates.json"
      to: "docs/runbooks/phase2-parity-gates.md"
      via: "documented command tiers"
      pattern: "pr|protected_branch|nightly"
    - from: "scripts/run_phase2_parity_gates.py"
      to: "config/verification/phase2-parity-gates.json"
      via: "tier execution lookup"
      pattern: "--tier"
---

<objective>
Create explicit CI-ready gate tiers for Phase 2 so parity-critical failures can block progress through stable repository-native commands and documentation.

Purpose: satisfy `PAR-04` after the full Phase 2 checkpoint and governance surface is explicit.
Output: gate-tier manifest, runner script, runbook, and contract tests.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-CONTEXT.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-RESEARCH.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-VALIDATION.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-04-PLAN.md
@docs/gsd/reviews/2026-04-12-phase2-gray-area-review.md

<interfaces>
Phase 2 approved gate tiers:
- PR: affected tests + intake-tolerance tests + minimum-skeleton failure tests + `annuity_performance` replay + explainability retrieval
- Protected branch: all accepted replay slices, including explicit `reference_derivation` checkpoint coverage
- Nightly or release: full `uv run pytest -v`

Existing commands to reuse:
- `uv run pytest tests/integration/test_phase2_intake_validation.py tests/replay/test_annuity_performance_slice.py tests/replay/test_phase2_annuity_performance_gates.py tests/replay/test_annuity_performance_explainability_slo.py -v`
- `uv run pytest tests/replay/test_annuity_performance_slice.py tests/replay/test_annual_award_slice.py tests/replay/test_annual_loss_slice.py tests/replay/test_phase2_reference_derivation_gates.py -v`
- `uv run pytest -v`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the machine-readable gate-tier manifest</name>
  <files>config/verification/phase2-parity-gates.json</files>
  <read_first>
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-CONTEXT.md
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-RESEARCH.md
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-VALIDATION.md
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-04-PLAN.md
    docs/gsd/reviews/2026-04-12-phase2-gray-area-review.md
  </read_first>
  <action>Create `config/verification/phase2-parity-gates.json` with exactly three top-level tiers: `pr`, `protected_branch`, and `nightly`. The `pr` tier must list commands for targeted contract/integration checks, annuity-performance replay, and annuity explainability retrieval. The `protected_branch` tier must list replay commands for `annuity_performance`, `annual_award`, and `annual_loss`, plus the explicit `reference_derivation` checkpoint coverage introduced by Plan 02-04. The `nightly` tier must list `uv run pytest -v` as the full-suite command.</action>
  <acceptance_criteria>Run `rg -n "\"pr\"|\"protected_branch\"|\"nightly\"|test_annuity_performance_slice|test_annual_award_slice|test_annual_loss_slice|test_phase2_reference_derivation_gates|uv run pytest -v" config/verification/phase2-parity-gates.json` and confirm all required tiers and commands exist.</acceptance_criteria>
  <verify>
    <automated>rg -n "\"pr\"|\"protected_branch\"|\"nightly\"|test_annuity_performance_slice|test_annual_award_slice|test_annual_loss_slice|test_phase2_reference_derivation_gates|uv run pytest -v" config/verification/phase2-parity-gates.json</automated>
  </verify>
  <done>Phase 2 gate tiers are explicit and machine-readable.</done>
</task>

<task type="auto">
  <name>Task 2: Add an executable gate-tier runner</name>
  <files>scripts/run_phase2_parity_gates.py</files>
  <read_first>
    config/verification/phase2-parity-gates.json
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-VALIDATION.md
  </read_first>
  <action>Create `scripts/run_phase2_parity_gates.py` that accepts `--tier pr`, `--tier protected_branch`, or `--tier nightly`, loads `config/verification/phase2-parity-gates.json`, and executes each listed command through `uv`-compatible subprocess calls in order. The script must print which tier is running and fail fast on the first non-zero command exit so CI can block immediately.</action>
  <acceptance_criteria>Run `rg -n "argparse|--tier|pr|protected_branch|nightly|subprocess" scripts/run_phase2_parity_gates.py` and confirm the runner supports all three tiers and fail-fast execution.</acceptance_criteria>
  <verify>
    <automated>rg -n "argparse|--tier|pr|protected_branch|nightly|subprocess" scripts/run_phase2_parity_gates.py</automated>
  </verify>
  <done>A repository-native runner exists for CI or local execution of the approved Phase 2 gate tiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Document and test the CI gate matrix</name>
  <files>docs/runbooks/phase2-parity-gates.md, tests/contracts/test_phase2_ci_gate_matrix.py</files>
  <read_first>
    config/verification/phase2-parity-gates.json
    scripts/run_phase2_parity_gates.py
    docs/runbooks/annuity-performance-replay.md
    .planning/phases/02-transparent-pipeline-contracts-parity-gates/02-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the manifest defines all three required tiers.
    - Test 2: each tier contains at least one command.
    - Test 3: the runner accepts all three tier names.
    - Test 4: the runbook describes what each tier blocks and when to use it.
  </behavior>
  <action>Write `docs/runbooks/phase2-parity-gates.md` with one section each for `pr`, `protected_branch`, and `nightly`, including the exact commands from the manifest and the blocking intent of each tier. Add `tests/contracts/test_phase2_ci_gate_matrix.py` asserting the manifest contains the three tiers, each tier has commands, and the runner source includes `--tier pr`, `--tier protected_branch`, and `--tier nightly`.</action>
  <acceptance_criteria>Run `uv run pytest tests/contracts/test_phase2_ci_gate_matrix.py -v` and confirm pass, then run `rg -n "## PR Gate|## Protected Branch Gate|## Nightly Gate|run_phase2_parity_gates.py" docs/runbooks/phase2-parity-gates.md` and confirm the runbook documents each tier.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_phase2_ci_gate_matrix.py -v</automated>
    <automated>rg -n "## PR Gate|## Protected Branch Gate|## Nightly Gate|run_phase2_parity_gates.py" docs/runbooks/phase2-parity-gates.md</automated>
  </verify>
  <done>Phase 2 CI gate tiers are documented for humans and enforced structurally for machines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest-defined gate tiers -> CI invocation | Missing or incorrect commands could give false confidence that parity-critical checks ran. |
| runner script -> CI blocking behavior | Non-fail-fast execution could allow later commands to mask earlier blocking failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | T | gate-tier manifest | mitigate | Contract-test the exact three required tiers and command presence, including explicit derivation-checkpoint coverage on protected branches. |
| T-02-14 | E | gate runner | mitigate | Make the runner fail fast on non-zero exit codes so CI cannot continue after a blocking failure. |
| T-02-15 | R | runbook and manifest drift | mitigate | Assert documented tier names and runner references exist in contract tests and runbook grep checks. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_phase2_ci_gate_matrix.py -v`
- `rg -n "## PR Gate|## Protected Branch Gate|## Nightly Gate|run_phase2_parity_gates.py" docs/runbooks/phase2-parity-gates.md`
</verification>

<success_criteria>
- CI-ready Phase 2 gate tiers are explicit, executable, and test-covered.
- PR, protected-branch, and nightly tiers match the completed Phase 2 scope.
- The repository can block on parity-critical failures without depending on a provider-specific CI config file.
</success_criteria>

<output>
After completion, create `.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-05-SUMMARY.md`
</output>
