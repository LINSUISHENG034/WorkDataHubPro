---
phase: 05-performance-reliability-optimization-with-drift-safeguards
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - docs/runbooks/performance-verification-matrix.md
  - scripts/run_perf_matrix.py
  - reference/perf-baselines/smoke.json
  - reference/perf-baselines/standard.json
  - reference/perf-baselines/large.json
  - tests/contracts/test_perf_matrix_contracts.py
autonomous: true
requirements:
  - PERF-03
must_haves:
  truths:
    - "The repo has one committed verification matrix that binds `smoke`, `standard`, and `large` tiers to exact commands, cadence, baselines, and thresholds."
    - "`scripts/run_perf_matrix.py` is the authoritative local/CI dispatcher for perf tiers rather than a marker-only convention."
    - "Committed perf baseline JSON assets exist under `reference/perf-baselines/` and use relative-threshold semantics rather than absolute-millisecond gates."
  artifacts:
    - path: "docs/runbooks/performance-verification-matrix.md"
      provides: "Human-readable workload-tier matrix for PR, protected-branch, and nightly verification."
      contains: "smoke,standard,large,PR,protected-branch,nightly,reference/perf-baselines"
    - path: "scripts/run_perf_matrix.py"
      provides: "Authoritative dispatcher for running a chosen perf tier and evaluating relative thresholds."
      contains: "--tier smoke,--tier standard,--tier large,reference/perf-baselines"
    - path: "tests/contracts/test_perf_matrix_contracts.py"
      provides: "Drift protection for matrix doc, dispatcher surface, and baseline assets."
      contains: "test_perf_matrix_runbook_references_exact_tiers,test_perf_dispatcher_exposes_tier_options,test_perf_baseline_assets_use_relative_thresholds"
  key_links:
    - from: "docs/runbooks/performance-verification-matrix.md"
      to: "scripts/run_perf_matrix.py"
      via: "the runbook documents the exact dispatcher entrypoint and tier mapping"
      pattern: "run_perf_matrix.py|smoke|standard|large"
    - from: "scripts/run_perf_matrix.py"
      to: "reference/perf-baselines/smoke.json"
      via: "dispatcher loads committed baseline assets and applies relative thresholds"
      pattern: "reference/perf-baselines|threshold_ratio|p50|p95|peak_memory"
    - from: "tests/contracts/test_perf_matrix_contracts.py"
      to: "docs/runbooks/performance-verification-matrix.md"
      via: "contract tests freeze the tier names, gate mapping, and baseline asset paths"
      pattern: "smoke|standard|large|PR|protected-branch|nightly"
---

<objective>
Turn Phase 5 verification into a committed, executable workload-tier matrix with governed baseline assets.

Purpose: satisfy PERF-03 by binding the phase’s hotspot and policy work to explicit verification tiers, baseline files, and one authoritative dispatcher.
Output: a matrix runbook, a dispatcher script, committed perf baseline JSON assets, and contract tests that prevent drift between them.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-CONTEXT.md
@.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-RESEARCH.md
@.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-VALIDATION.md
@.planning/codebase/TESTING.md
@docs/disciplines/implementation-toolchain.md
@tests/performance/test_trace_lookup_micro_benchmark.py
@tests/integration/test_publication_service.py

<interfaces>
Locked verification rules:
- tiers are exactly `smoke`, `standard`, `large`
- mapping is exactly `smoke@PR`, `standard@protected-branch`, `large@nightly`
- baselines live under `reference/perf-baselines/`
- thresholds are relative ratios, not absolute milliseconds
- dispatcher surface is authoritative; pytest markers are not the primary mechanism
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the committed workload-tier verification matrix runbook with exact gate mapping and command forms</name>
  <files>docs/runbooks/performance-verification-matrix.md</files>
  <read_first>
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-CONTEXT.md
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-RESEARCH.md
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-VALIDATION.md
    .planning/codebase/TESTING.md
  </read_first>
  <action>Create `docs/runbooks/performance-verification-matrix.md` with an explicit matrix that names the exact tiers `smoke`, `standard`, and `large`; the exact gate mapping `smoke@PR`, `standard@protected-branch`, and `large@nightly`; and the exact committed baseline root `reference/perf-baselines/`. Include one table with the exact columns `Tier`, `Primary scope`, `Command`, `Cadence`, `Baseline`, and `Threshold rule`. For `smoke`, document a fast local/PR command routed through `uv run python scripts/run_perf_matrix.py --tier smoke`; for `standard`, document `uv run python scripts/run_perf_matrix.py --tier standard`; for `large`, document `uv run python scripts/run_perf_matrix.py --tier large`. Make the tier-to-command mapping explicit by pairing each tier with its exact dispatcher invocation and cadence in the table. In the threshold rule column, state that each tier uses relative ratios against committed baselines and must not exceed `baseline * threshold_ratio` for `p50`, `p95`, and `peak_memory` metrics. Do not describe pytest markers as the authoritative dispatcher.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_perf_matrix_contracts.py -v` passes, proving the runbook stays aligned with the exact tiers, cadence mapping, baseline root, and dispatcher commands, and `rg -n "smoke|standard|large|PR|protected-branch|nightly|reference/perf-baselines|baseline \* threshold_ratio|scripts/run_perf_matrix.py --tier" docs/runbooks/performance-verification-matrix.md` returns hits for every required tier, cadence, baseline root, threshold rule, and dispatcher command.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_perf_matrix_contracts.py -v</automated>
    <automated>rg -n "smoke|standard|large|PR|protected-branch|nightly|reference/perf-baselines|baseline \* threshold_ratio|scripts/run_perf_matrix.py --tier" docs/runbooks/performance-verification-matrix.md</automated>
  </verify>
  <done>The repo now has one human-readable matrix that states exactly what runs when, against which baselines, and under which threshold semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add committed baseline JSON assets and one authoritative perf-tier dispatcher</name>
  <files>scripts/run_perf_matrix.py, reference/perf-baselines/smoke.json, reference/perf-baselines/standard.json, reference/perf-baselines/large.json</files>
  <read_first>
    docs/runbooks/performance-verification-matrix.md
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-RESEARCH.md
    tests/performance/test_trace_lookup_micro_benchmark.py
    tests/integration/test_publication_service.py
  </read_first>
  <action>Create `scripts/run_perf_matrix.py` as the authoritative dispatcher for the exact CLI shape `uv run python scripts/run_perf_matrix.py --tier smoke|standard|large`. Implement the script so each tier maps to an explicit command list and a baseline JSON file under `reference/perf-baselines/`. Lock the tier-to-command mapping as follows: `smoke` runs `uv run pytest tests/integration/test_publication_service.py tests/performance/test_trace_lookup_micro_benchmark.py tests/performance/test_contract_state_projection_benchmark.py tests/contracts/test_perf_matrix_contracts.py -v`; `standard` runs `uv run pytest tests/replay/test_annuity_performance_slice.py tests/replay/test_annual_award_slice.py tests/replay/test_annual_loss_slice.py tests/replay/test_annuity_performance_explainability_slo.py tests/replay/test_annual_award_explainability_slo.py tests/replay/test_annual_loss_explainability_slo.py -v`; `large` runs `uv run pytest -v`. Create `reference/perf-baselines/smoke.json`, `reference/perf-baselines/standard.json`, and `reference/perf-baselines/large.json` with a common structure that includes the exact top-level keys `tier`, `threshold_ratio`, and `metrics`. Under `metrics`, include the exact metric keys `p50`, `p95`, and `peak_memory`. Use relative-threshold semantics only; do not encode absolute pass/fail milliseconds outside the baseline metrics themselves. The dispatcher must load the baseline for the chosen tier, run the mapped commands, collect actual measured metrics in a repo-executable way, and exit non-zero when any measured metric exceeds `baseline * threshold_ratio`.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_perf_matrix_contracts.py -v` passes, proving the dispatcher surface and baseline asset schema stay synchronized, and `rg -n "--tier smoke|--tier standard|--tier large|tests/integration/test_publication_service.py|tests/performance/test_trace_lookup_micro_benchmark.py|tests/performance/test_contract_state_projection_benchmark.py|tests/replay/test_annuity_performance_slice.py|tests/replay/test_annual_award_slice.py|tests/replay/test_annual_loss_slice.py|tests/replay/test_annuity_performance_explainability_slo.py|tests/replay/test_annual_award_explainability_slo.py|tests/replay/test_annual_loss_explainability_slo.py|uv run pytest -v|reference/perf-baselines|threshold_ratio|p50|p95|peak_memory" scripts/run_perf_matrix.py reference/perf-baselines/smoke.json reference/perf-baselines/standard.json reference/perf-baselines/large.json` returns hits for the locked dispatcher options, exact tier command lists, and baseline fields.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_perf_matrix_contracts.py -v</automated>
    <automated>rg -n "--tier smoke|--tier standard|--tier large|tests/integration/test_publication_service.py|tests/performance/test_trace_lookup_micro_benchmark.py|tests/performance/test_contract_state_projection_benchmark.py|tests/replay/test_annuity_performance_slice.py|tests/replay/test_annual_award_slice.py|tests/replay/test_annual_loss_slice.py|tests/replay/test_annuity_performance_explainability_slo.py|tests/replay/test_annual_award_explainability_slo.py|tests/replay/test_annual_loss_explainability_slo.py|uv run pytest -v|reference/perf-baselines|threshold_ratio|p50|p95|peak_memory" scripts/run_perf_matrix.py reference/perf-baselines/smoke.json reference/perf-baselines/standard.json reference/perf-baselines/large.json</automated>
  </verify>
  <done>The phase now has real baseline assets and one executable entrypoint instead of a documentation-only performance story.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract tests that freeze tier names, dispatcher surface, and relative-threshold baseline semantics</name>
  <files>tests/contracts/test_perf_matrix_contracts.py</files>
  <read_first>
    docs/runbooks/performance-verification-matrix.md
    scripts/run_perf_matrix.py
    reference/perf-baselines/smoke.json
    reference/perf-baselines/standard.json
    reference/perf-baselines/large.json
  </read_first>
  <action>Create `tests/contracts/test_perf_matrix_contracts.py` with the exact tests `test_perf_matrix_runbook_references_exact_tiers`, `test_perf_dispatcher_exposes_tier_options`, and `test_perf_baseline_assets_use_relative_thresholds`. `test_perf_matrix_runbook_references_exact_tiers` must assert the runbook contains `smoke`, `standard`, `large`, `PR`, `protected-branch`, `nightly`, `reference/perf-baselines`, and `scripts/run_perf_matrix.py --tier`. `test_perf_dispatcher_exposes_tier_options` must assert the dispatcher surface exposes the exact `--tier smoke`, `--tier standard`, and `--tier large` options. `test_perf_baseline_assets_use_relative_thresholds` must load each baseline JSON and assert it contains `tier`, `threshold_ratio`, and `metrics`, and that `metrics` contains `p50`, `p95`, and `peak_memory`. The test must also assert `threshold_ratio` is numeric and greater than `1.0` so the baseline rule is genuinely relative. Finish by running `uv run pytest tests/contracts/test_perf_matrix_contracts.py -v`.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_perf_matrix_contracts.py -v` passes. `rg -n "test_perf_matrix_runbook_references_exact_tiers|test_perf_dispatcher_exposes_tier_options|test_perf_baseline_assets_use_relative_thresholds|threshold_ratio|peak_memory|protected-branch|nightly" tests/contracts/test_perf_matrix_contracts.py` returns hits for every required contract test and validation field.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_perf_matrix_contracts.py -v</automated>
    <automated>rg -n "test_perf_matrix_runbook_references_exact_tiers|test_perf_dispatcher_exposes_tier_options|test_perf_baseline_assets_use_relative_thresholds|threshold_ratio|peak_memory|protected-branch|nightly" tests/contracts/test_perf_matrix_contracts.py</automated>
  </verify>
  <done>The matrix, dispatcher, and baseline assets are all protected by executable contract tests rather than relying on documentation drift detection by humans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook matrix -> CI/local execution | If the documented tiers drift from the dispatcher or baseline assets, PERF-03 is only partially true. |
| committed baseline assets -> perf pass/fail behavior | Baselines must encode relative-threshold semantics clearly enough that the dispatcher can enforce them consistently. |
| dispatcher surface -> project toolchain | The perf matrix must remain executable through repo-native commands and not depend on hidden local conventions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | R | perf matrix governance surface | mitigate | Freeze exact tier names, cadence mapping, dispatcher options, and baseline fields in contract tests. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_perf_matrix_contracts.py -v`
- `uv run pytest -v`
</verification>

<success_criteria>
- `docs/runbooks/performance-verification-matrix.md` documents the exact tier mapping and threshold rule.
- `scripts/run_perf_matrix.py` exists as the authoritative tier dispatcher.
- `reference/perf-baselines/` contains committed JSON assets with relative-threshold semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-04-SUMMARY.md`
</output>
