---
phase: 05-performance-reliability-optimization-with-drift-safeguards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/work_data_hub_pro/capabilities/projections/contract_state.py
  - tests/performance/test_contract_state_projection_benchmark.py
  - tests/integration/test_projection_outputs.py
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "`ContractStateProjection.run()` produces the same projection booleans and row shape as before while replacing repeated linear membership scans with pre-built indexes."
    - "The optimization stays in-place inside `src/work_data_hub_pro/capabilities/projections/contract_state.py` with no public signature changes and no feature flag."
    - "Projection hotspot behavior remains parity-safe because replay acceptance tests still pass after the change."
  artifacts:
    - path: "src/work_data_hub_pro/capabilities/projections/contract_state.py"
      provides: "Indexed membership lookups for award/loss fact and fixture rows without changing projection semantics."
      contains: "_has_match,company_id,plan_code,period,has_annual_award_fact,has_annual_award_fixture,has_annual_loss_fact,has_annual_loss_fixture"
    - path: "tests/performance/test_contract_state_projection_benchmark.py"
      provides: "Hotspot-level benchmark and semantic guard for contract-state membership indexing."
      contains: "test_contract_state_projection_benchmark_stays_within_threshold,test_contract_state_projection_matches_fixture_expectations"
    - path: "tests/integration/test_projection_outputs.py"
      provides: "Regression proof that projection output booleans remain unchanged for representative fact/fixture combinations."
      contains: "contract_state,has_annual_award_fact,has_annual_award_fixture,has_annual_loss_fact,has_annual_loss_fixture"
  key_links:
    - from: "src/work_data_hub_pro/capabilities/projections/contract_state.py"
      to: "src/work_data_hub_pro/platform/storage/in_memory_tables.py"
      via: "reads fact and fixture rows, then derives membership indexes from existing table contents"
      pattern: "read(\"fact_annuity_performance\")|read(\"fact_annual_award\")|read(\"fixture_annual_award\")|read(\"fact_annual_loss\")|read(\"fixture_annual_loss\")"
    - from: "tests/performance/test_contract_state_projection_benchmark.py"
      to: "src/work_data_hub_pro/capabilities/projections/contract_state.py"
      via: "benchmark and semantic fixture assertions exercise the in-place indexed implementation"
      pattern: "ContractStateProjection|_has_match|run"
    - from: "tests/integration/test_projection_outputs.py"
      to: "src/work_data_hub_pro/capabilities/projections/contract_state.py"
      via: "regression assertions freeze output booleans for fact/fixture membership combinations"
      pattern: "has_annual_award_fact|has_annual_award_fixture|has_annual_loss_fact|has_annual_loss_fixture"
---

<objective>
Eliminate the contract-state repeated linear-scan hotspot without changing projection semantics.

Purpose: satisfy the projection side of PERF-01 with one isolated, in-place optimization that can be validated independently and reverted independently.
Output: indexed membership lookups in `ContractStateProjection`, one dedicated performance benchmark, and regression coverage proving the projection booleans did not drift.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-CONTEXT.md
@.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-RESEARCH.md
@.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-VALIDATION.md
@.planning/codebase/CONCERNS.md
@src/work_data_hub_pro/capabilities/projections/contract_state.py
@src/work_data_hub_pro/platform/storage/in_memory_tables.py
@tests/integration/test_projection_outputs.py

<interfaces>
Current hotspot shape:
- `_has_match(rows, company_id, plan_code, period)` does `any(...)` over the full row list.
- `run(...)` calls `_has_match(...)` four times per annuity-performance row.

Locked implementation constraints:
- in-place change only
- no new adapter type
- no public signature changes
- final drift arbiter is the replay suite
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace repeated list scans with pre-built membership indexes inside `ContractStateProjection.run`</name>
  <files>src/work_data_hub_pro/capabilities/projections/contract_state.py</files>
  <read_first>
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-CONTEXT.md
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-RESEARCH.md
    src/work_data_hub_pro/capabilities/projections/contract_state.py
    src/work_data_hub_pro/platform/storage/in_memory_tables.py
  </read_first>
  <action>Per D-01 through D-04, keep the change in `src/work_data_hub_pro/capabilities/projections/contract_state.py` and do not change any public signature. Build membership indexes from the already-read fact and fixture row lists using the exact tuple shape `(company_id, plan_code, period)`. Precompute four exact lookup sets from `award_fact_rows`, `award_fixture_rows`, `loss_fact_rows`, and `loss_fixture_rows`, then make `_has_match(...)` perform membership lookup against a pre-built set instead of re-scanning a list. Preserve the existing short-circuit semantics exactly: `has_award_fixture = has_award_fact or fixture_hit` and `has_loss_fixture = has_loss_fact or fixture_hit`. Do not move business semantics into a generic helper module, do not add a feature flag, and do not touch unrelated projection logic.</action>
  <acceptance_criteria>`uv run pytest tests/performance/test_contract_state_projection_benchmark.py tests/integration/test_projection_outputs.py -v` passes, proving the indexed implementation preserves the existing projection booleans while replacing repeated scans. `rg -n "company_id.*plan_code.*period|has_annual_award_fact|has_annual_award_fixture|has_annual_loss_fact|has_annual_loss_fixture" src/work_data_hub_pro/capabilities/projections/contract_state.py` returns hits for the tuple-based index and all four projection booleans.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/performance/test_contract_state_projection_benchmark.py tests/integration/test_projection_outputs.py -v</automated>
    <automated>rg -n "company_id.*plan_code.*period|has_annual_award_fact|has_annual_award_fixture|has_annual_loss_fact|has_annual_loss_fixture" src/work_data_hub_pro/capabilities/projections/contract_state.py</automated>
  </verify>
  <done>The hotspot logic is reduced to pre-built membership lookups without any outward API or behavior contract changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated performance benchmark and fixture-based semantic guard for contract-state projection</name>
  <files>tests/performance/test_contract_state_projection_benchmark.py</files>
  <read_first>
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-CONTEXT.md
    src/work_data_hub_pro/capabilities/projections/contract_state.py
    tests/performance/test_trace_lookup_micro_benchmark.py
  </read_first>
  <action>Create `tests/performance/test_contract_state_projection_benchmark.py` with the exact tests `test_contract_state_projection_benchmark_stays_within_threshold` and `test_contract_state_projection_matches_fixture_expectations`. Follow the existing micro-benchmark style in `tests/performance/test_trace_lookup_micro_benchmark.py`: build an in-memory dataset large enough to exercise the former repeated-scan path, run `ContractStateProjection.run(...)`, assert representative output booleans for at least one row with both fact and fixture hits, one row with fact-only hits, and one row with no event-domain hits, then assert the elapsed time stays below a concrete threshold suitable for the local in-memory benchmark. The semantic guard must freeze the exact fields `company_id`, `plan_code`, `period`, `has_annuity_performance`, `has_annual_award_fact`, `has_annual_award_fixture`, `has_annual_loss_fact`, and `has_annual_loss_fixture` for the fixture rows.</action>
  <acceptance_criteria>`uv run pytest tests/performance/test_contract_state_projection_benchmark.py -v` passes. `rg -n "test_contract_state_projection_benchmark_stays_within_threshold|test_contract_state_projection_matches_fixture_expectations|has_annual_award_fact|has_annual_award_fixture|has_annual_loss_fact|has_annual_loss_fixture" tests/performance/test_contract_state_projection_benchmark.py` returns hits for both required tests and all projection booleans.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/performance/test_contract_state_projection_benchmark.py -v</automated>
    <automated>rg -n "test_contract_state_projection_benchmark_stays_within_threshold|test_contract_state_projection_matches_fixture_expectations|has_annual_award_fact|has_annual_award_fixture|has_annual_loss_fact|has_annual_loss_fixture" tests/performance/test_contract_state_projection_benchmark.py</automated>
  </verify>
  <done>The hotspot has a local benchmark and a fixture-style semantic proof instead of relying on intuition about faster code.</done>
</task>

<task type="auto">
  <name>Task 3: Prove projection outputs remain parity-safe on the integration and replay boundaries</name>
  <files>tests/integration/test_projection_outputs.py</files>
  <read_first>
    .planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-CONTEXT.md
    src/work_data_hub_pro/capabilities/projections/contract_state.py
    tests/integration/test_projection_outputs.py
  </read_first>
  <action>Extend `tests/integration/test_projection_outputs.py` so contract-state assertions explicitly cover the exact combinations that the indexed lookup could break: fact hit plus fixture hit, fact hit plus no fixture hit, fixture-only hit through the `or` path, and complete miss. Keep the test at the existing integration boundary using `InMemoryTableStore` and `ContractStateProjection`; do not add a second implementation or `_legacy()` shim. After the targeted integration assertions are in place, this plan’s completion verification must also run the replay acceptance suite for `annuity_performance`, `annual_award`, and `annual_loss` because D-05/D-08 say replay remains the final drift arbiter.</action>
  <acceptance_criteria>`uv run pytest tests/integration/test_projection_outputs.py -v` passes. `uv run pytest tests/replay/test_annuity_performance_slice.py tests/replay/test_annual_award_slice.py tests/replay/test_annual_loss_slice.py -v` passes. `tests/integration/test_projection_outputs.py` contains assertions for `has_annual_award_fact`, `has_annual_award_fixture`, `has_annual_loss_fact`, and `has_annual_loss_fixture`.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_projection_outputs.py -v</automated>
    <automated>uv run pytest tests/replay/test_annuity_performance_slice.py tests/replay/test_annual_award_slice.py tests/replay/test_annual_loss_slice.py -v</automated>
  </verify>
  <done>The indexed projection path is proven on both the direct integration boundary and the replay parity boundary that governs the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fact / fixture rows -> projection membership logic | Index construction must not change the meaning of fact-hit vs fixture-hit booleans. |
| projection hotspot optimization -> replay parity gates | A faster membership path must not silently drift from accepted replay outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | contract-state membership logic | mitigate | Freeze the tuple key shape `(company_id, plan_code, period)` and assert exact projection booleans in benchmark and integration tests. |
| T-05-02 | R | parity drift after optimization | mitigate | Require the targeted integration test plus the three replay slice acceptance tests before plan completion. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/performance/test_contract_state_projection_benchmark.py tests/integration/test_projection_outputs.py -v`
- `uv run pytest tests/replay/test_annuity_performance_slice.py tests/replay/test_annual_award_slice.py tests/replay/test_annual_loss_slice.py -v`
</verification>

<success_criteria>
- `ContractStateProjection` uses pre-built membership indexes over `(company_id, plan_code, period)`.
- A dedicated performance benchmark exists for the contract-state hotspot.
- Integration and replay tests prove the projection booleans did not drift.
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-reliability-optimization-with-drift-safeguards/05-01-SUMMARY.md`
</output>
