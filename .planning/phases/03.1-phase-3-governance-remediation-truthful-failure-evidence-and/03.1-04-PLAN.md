---
phase: 03.1-phase-3-governance-remediation-truthful-failure-evidence-and
plan: 04
type: gap-closure
wave: 1
depends_on: []
files_modified:
  - tests/replay/test_annuity_performance_slice.py
  - tests/replay/test_annual_award_slice.py
  - tests/replay/test_annual_loss_slice.py
autonomous: true
requirements:
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "Failed replay runs create compatibility cases whose sample_locator, legacy_result, and pro_result match the same blocking checkpoint named by primary_failure.checkpoint_name."
  artifacts:
    - path: "tests/replay/test_annuity_performance_slice.py"
      provides: "Truthful failed-checkpoint payload selection test helper."
      contains: "def _bootstrap_intermediate_baselines(... -> dict[str, list[dict[str, object]]])"
    - path: "tests/replay/test_annual_award_slice.py"
      provides: "Truthful failed-checkpoint payload selection test helper."
      contains: "def _bootstrap_intermediate_baselines(... -> dict[str, list[dict[str, object]]])"
    - path: "tests/replay/test_annual_loss_slice.py"
      provides: "Truthful failed-checkpoint payload selection test helper."
      contains: "def _bootstrap_intermediate_baselines(... -> dict[str, list[dict[str, object]]])"
---

<objective>
Fix `_bootstrap_intermediate_baselines` test helper in three slice test files so it returns `outcome.intermediate_payloads` instead of None.

Purpose: close VERIFICATION.md Gap — the helper was declared `-> None` with no return statement, causing the 3 new truthful-failure-evidence tests to fail with `TypeError: 'NoneType' object is not subscriptable`.
Output: all three slice test files have the correct return type and return statement, matching the reference implementation in `tests/replay/test_phase2_event_domain_gates.py`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix _bootstrap_intermediate_baselines return type and return statement in test_annuity_performance_slice.py</name>
  <files>tests/replay/test_annuity_performance_slice.py</files>
  <read_first>
    tests/replay/test_annuity_performance_slice.py
    tests/replay/test_phase2_event_domain_gates.py
  </read_first>
  <action>Change `def _bootstrap_intermediate_baselines(workbook_path: Path, replay_root: Path) -> None:` to `def _bootstrap_intermediate_baselines(workbook_path: Path, replay_root: Path) -> dict[str, list[dict[str, object]]]:` on line 96. Add `return outcome.intermediate_payloads` after line 125 (after the second for loop that writes baseline files, before the function ends).</action>
  <acceptance_criteria>`uv run pytest tests/replay/test_annuity_performance_slice.py::test_annuity_performance_slice_replay_uses_failed_checkpoint_payloads -v` passes. `rg -n "def _bootstrap_intermediate_baselines.*-> dict\[str, list\[dict\[str, object\]\]" tests/replay/test_annuity_performance_slice.py` returns a hit. `rg -n "return outcome.intermediate_payloads" tests/replay/test_annuity_performance_slice.py` returns a hit.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_annuity_performance_slice.py::test_annuity_performance_slice_replay_uses_failed_checkpoint_payloads -v</automated>
  </verify>
  <done>_bootstrap_intermediate_baselines returns the intermediate_payloads dict.</done>
</task>

<task type="auto">
  <name>Task 2: Fix _bootstrap_intermediate_baselines return type and return statement in test_annual_award_slice.py</name>
  <files>tests/replay/test_annual_award_slice.py</files>
  <read_first>
    tests/replay/test_annual_award_slice.py
    tests/replay/test_phase2_event_domain_gates.py
  </read_first>
  <action>Change `def _bootstrap_intermediate_baselines(workbook_path: Path, replay_root: Path) -> None:` to `def _bootstrap_intermediate_baselines(workbook_path: Path, replay_root: Path) -> dict[str, list[dict[str, object]]]:` on line 133. Add `return outcome.intermediate_payloads` after line 162 (after the second for loop that writes baseline files, before the function ends).</action>
  <acceptance_criteria>`uv run pytest tests/replay/test_annual_award_slice.py::test_annual_award_slice_replay_uses_failed_checkpoint_payloads -v` passes. `rg -n "def _bootstrap_intermediate_baselines.*-> dict\[str, list\[dict\[str, object\]\]" tests/replay/test_annual_award_slice.py` returns a hit. `rg -n "return outcome.intermediate_payloads" tests/replay/test_annual_award_slice.py` returns a hit.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_annual_award_slice.py::test_annual_award_slice_replay_uses_failed_checkpoint_payloads -v</automated>
  </verify>
  <done>_bootstrap_intermediate_baselines returns the intermediate_payloads dict.</done>
</task>

<task type="auto">
  <name>Task 3: Fix _bootstrap_intermediate_baselines return type and return statement in test_annual_loss_slice.py</name>
  <files>tests/replay/test_annual_loss_slice.py</files>
  <read_first>
    tests/replay/test_annual_loss_slice.py
    tests/replay/test_phase2_event_domain_gates.py
  </read_first>
  <action>Change `def _bootstrap_intermediate_baselines(workbook_path: Path, replay_root: Path) -> None:` to `def _bootstrap_intermediate_baselines(workbook_path: Path, replay_root: Path) -> dict[str, list[dict[str, object]]]:` on line 188. Add `return outcome.intermediate_payloads` after line 217 (after the second for loop that writes baseline files, before the function ends).</action>
  <acceptance_criteria>`uv run pytest tests/replay/test_annual_loss_slice.py::test_annual_loss_slice_replay_uses_failed_checkpoint_payloads -v` passes. `rg -n "def _bootstrap_intermediate_baselines.*-> dict\[str, list\[dict\[str, object\]\]" tests/replay/test_annual_loss_slice.py` returns a hit. `rg -n "return outcome.intermediate_payloads" tests/replay/test_annual_loss_slice.py` returns a hit.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_annual_loss_slice.py::test_annual_loss_slice_replay_uses_failed_checkpoint_payloads -v</automated>
  </verify>
  <done>_bootstrap_intermediate_baselines returns the intermediate_payloads dict.</done>
</task>

</tasks>

<verification>
Run:
- `uv run pytest tests/replay/test_annuity_performance_slice.py::test_annuity_performance_slice_replay_uses_failed_checkpoint_payloads tests/replay/test_annual_award_slice.py::test_annual_award_slice_replay_uses_failed_checkpoint_payloads tests/replay/test_annual_loss_slice.py::test_annual_loss_slice_replay_uses_failed_checkpoint_payloads -v`
</verification>

<success_criteria>
- All three _bootstrap_intermediate_baselines functions have `-> dict[str, list[dict[str, object]]]` return type.
- All three functions contain `return outcome.intermediate_payloads`.
- All three `test_*_replay_uses_failed_checkpoint_payloads` tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-04-SUMMARY.md`
</output>