---
phase: 03.1-phase-3-governance-remediation-truthful-failure-evidence-and
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/work_data_hub_pro/governance/evidence_index/file_store.py
  - src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py
  - src/work_data_hub_pro/apps/etl_cli/main.py
  - tests/contracts/test_replay_diagnose_contracts.py
autonomous: true
requirements:
  - PIPE-03
  - OPS-01
  - PAR-02
must_haves:
  truths:
    - "Manifest-declared replay package paths cannot escape the current comparison-run package."
    - "Path-like invalid `comparison_run_id` values return a typed CLI error instead of a Python traceback."
    - "Successful `replay diagnose` behavior for valid runs and missing runs remains unchanged."
  artifacts:
    - path: "src/work_data_hub_pro/governance/evidence_index/file_store.py"
      provides: "Fail-closed comparison-run package-path resolution."
      contains: "_resolve_package_path_for_run"
    - path: "src/work_data_hub_pro/apps/etl_cli/main.py"
      provides: "Typed invalid-id diagnose CLI behavior."
      contains: "Invalid comparison_run_id"
    - path: "tests/contracts/test_replay_diagnose_contracts.py"
      provides: "Contract coverage for path-escape rejection and invalid-id CLI handling."
      contains: "test_load_replay_diagnostics_rejects_absolute_manifest_package_paths,test_load_replay_diagnostics_rejects_escaping_manifest_package_paths,test_replay_diagnose_cli_returns_typed_invalid_id_error"
  key_links:
    - from: "src/work_data_hub_pro/governance/evidence_index/file_store.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py"
      via: "shared fail-closed package-path resolution"
      pattern: "_resolve_package_path_for_run"
    - from: "src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py"
      to: "src/work_data_hub_pro/apps/etl_cli/main.py"
      via: "typed invalid-id and missing-run diagnose surface"
      pattern: "ValueError|ReplayDiagnosticsNotFoundError|replay_diagnose"
---

<objective>
Harden `replay diagnose` so it is fail-closed on manifest package paths and stable on invalid input.

Purpose: close the completion-audit defects where manifest-controlled package paths can point outside the current comparison-run package and invalid path-like identifiers surface raw Python tracebacks.
Output: governed package-path validation in the evidence-index reader path and typed CLI handling for invalid `comparison_run_id` values.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-CONTEXT.md
@.planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-RESEARCH.md
@.planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-VALIDATION.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-01-PLAN.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-05-SUMMARY.md
@src/work_data_hub_pro/governance/evidence_index/file_store.py
@src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py
@src/work_data_hub_pro/apps/etl_cli/main.py
@tests/contracts/test_replay_diagnose_contracts.py
@tests/contracts/test_replay_cli_contracts.py
@tests/contracts/test_phase2_gate_contracts.py

<interfaces>
Current behavior:
- `find_comparison_run_root(...)` rejects path separators in `comparison_run_id`
- manifest `package_paths` are resolved without staying inside the current run package
- the CLI catches missing-run diagnostics but not invalid-id `ValueError`

This plan must preserve successful output for valid runs and typed missing-run behavior while hardening path handling and invalid input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze fail-closed diagnose contracts for manifest path escapes and invalid CLI identifiers</name>
  <files>tests/contracts/test_replay_diagnose_contracts.py</files>
  <read_first>
    .planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-CONTEXT.md
    .planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-RESEARCH.md
    tests/contracts/test_replay_diagnose_contracts.py
    src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py
    src/work_data_hub_pro/governance/evidence_index/file_store.py
    src/work_data_hub_pro/apps/etl_cli/main.py
  </read_first>
  <action>Extend `tests/contracts/test_replay_diagnose_contracts.py` with three exact tests named `test_load_replay_diagnostics_rejects_absolute_manifest_package_paths`, `test_load_replay_diagnostics_rejects_escaping_manifest_package_paths`, and `test_replay_diagnose_cli_returns_typed_invalid_id_error`. The two manifest-path tests must create temporary comparison-run packages whose manifest `package_paths` point to an absolute path and to an escaping relative path such as `comparison_runs/<id>/../../outside.json`, then assert `load_replay_diagnostics(...)` raises `ValueError` with a message mentioning `package_paths`. The CLI test must invoke `replay diagnose --comparison-run-id ../evil`, assert exit code `1`, and assert stderr contains the exact substring `Invalid comparison_run_id: ../evil` without any traceback text.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_replay_diagnose_contracts.py tests/contracts/test_replay_cli_contracts.py tests/contracts/test_phase2_gate_contracts.py -v` passes. `rg -n "test_load_replay_diagnostics_rejects_absolute_manifest_package_paths|test_load_replay_diagnostics_rejects_escaping_manifest_package_paths|test_replay_diagnose_cli_returns_typed_invalid_id_error|Invalid comparison_run_id: \\../evil|package_paths" tests/contracts/test_replay_diagnose_contracts.py` returns hits for all three new tests and the required error-message assertions.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_replay_diagnose_contracts.py tests/contracts/test_replay_cli_contracts.py tests/contracts/test_phase2_gate_contracts.py -v</automated>
    <automated>rg -n "test_load_replay_diagnostics_rejects_absolute_manifest_package_paths|test_load_replay_diagnostics_rejects_escaping_manifest_package_paths|test_replay_diagnose_cli_returns_typed_invalid_id_error|Invalid comparison_run_id: \\../evil|package_paths" tests/contracts/test_replay_diagnose_contracts.py</automated>
  </verify>
  <done>The diagnose contract suite fails if manifest package paths escape the current run package or if invalid CLI identifiers surface a traceback.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fail-closed package-path resolution and typed invalid-id CLI handling</name>
  <files>src/work_data_hub_pro/governance/evidence_index/file_store.py, src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py, src/work_data_hub_pro/apps/etl_cli/main.py</files>
  <read_first>
    .planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-CONTEXT.md
    .planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-RESEARCH.md
    src/work_data_hub_pro/governance/evidence_index/file_store.py
    src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py
    src/work_data_hub_pro/apps/etl_cli/main.py
    tests/contracts/test_replay_diagnose_contracts.py
    tests/contracts/test_replay_cli_contracts.py
    tests/contracts/test_phase2_gate_contracts.py
  </read_first>
  <action>Add `_resolve_package_path_for_run(comparison_run_id: str, package_key: str, default_filename: str) -> Path` to `src/work_data_hub_pro/governance/evidence_index/file_store.py`. The helper must read the manifest, reject absolute `package_paths`, resolve relative paths against `self._root`, and then reject any resolved path that is not inside `self._comparison_run_root_path(comparison_run_id).resolve()`. Use this helper for gate summary, checkpoint results, publication results, compatibility case, and report readers. Update `src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py` so the generated `ReplayEvidencePaths` values also come from the same validated resolution logic. Update `src/work_data_hub_pro/apps/etl_cli/main.py` so `replay_diagnose(...)` catches `ValueError`, writes `Invalid comparison_run_id: {comparison_run_id}` to stderr, and exits with code `1` while preserving the existing missing-run handling and JSON success path.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_replay_diagnose_contracts.py tests/contracts/test_replay_cli_contracts.py tests/contracts/test_phase2_gate_contracts.py -v` passes. `rg -n "_resolve_package_path_for_run|Invalid comparison_run_id|ValueError|package_paths" src/work_data_hub_pro/governance/evidence_index/file_store.py src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py src/work_data_hub_pro/apps/etl_cli/main.py` returns hits for the new fail-closed helper and typed CLI invalid-id handling.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_replay_diagnose_contracts.py tests/contracts/test_replay_cli_contracts.py tests/contracts/test_phase2_gate_contracts.py -v</automated>
    <automated>rg -n "_resolve_package_path_for_run|Invalid comparison_run_id|ValueError|package_paths" src/work_data_hub_pro/governance/evidence_index/file_store.py src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py src/work_data_hub_pro/apps/etl_cli/main.py</automated>
  </verify>
  <done>`replay diagnose` is fail-closed on manifest package paths and stable on invalid path-like identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest `package_paths` -> file-backed evidence readers | Manifest data is untrusted input until resolved inside the current comparison-run package root. |
| CLI `comparison_run_id` -> diagnostics loader | Invalid path-like identifiers must be typed user errors, not raw implementation exceptions. |
| successful diagnose surface -> hardening changes | Path-hardening fixes must not change the JSON output contract for valid runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-03 | T | manifest package-path resolution | mitigate | Reject absolute and escaping relative paths and require all package files to resolve inside the current `comparison_runs/<comparison_run_id>/` root. |
| T-03.1-04 | I | diagnose CLI invalid-id behavior | mitigate | Catch `ValueError` at the CLI boundary, emit `Invalid comparison_run_id: {comparison_run_id}`, and verify stderr does not contain traceback output. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_replay_diagnose_contracts.py tests/contracts/test_replay_cli_contracts.py tests/contracts/test_phase2_gate_contracts.py -v`
</verification>

<success_criteria>
- Manifest-controlled package paths can no longer redirect diagnose reads outside the current comparison-run package.
- `replay diagnose --comparison-run-id ../evil` returns a typed stderr message and exit code `1` with no traceback.
- Valid `replay diagnose` JSON output and missing-run behavior remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-phase-3-governance-remediation-truthful-failure-evidence-and/03.1-02-SUMMARY.md`
</output>
