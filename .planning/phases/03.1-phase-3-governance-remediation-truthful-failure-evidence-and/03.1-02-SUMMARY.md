---
phase: 03.1-phase-3-governance-remediation-truthful-failure-evidence-and
plan: "02"
subsystem: governance
tags: [replay, diagnostics, path-hardening, CLI-contracts, evidence-index]

# Dependency graph
requires:
  - phase: 03-orchestration-refactor-failure-explainability
    provides: registry-backed replay CLI, typed missing-run diagnose surface
provides:
  - fail-closed package-path resolution in FileEvidenceIndex
  - typed ValueError from load_replay_diagnostics for path escapes
  - typed stderr message and exit-1 for invalid CLI comparison_run_id
affects:
  - 03.1-phase-3-governance-remediation-truthful-failure-evidence-and
  - 04-agent-operations-governance-hardening

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Fail-closed path resolution: resolve relative paths only inside the comparison-run root
    - Typed CLI boundary: catch ValueError at CLI surface, emit structured error to stderr

key-files:
  created:
    - tests/contracts/test_replay_diagnose_contracts.py (extended with 3 new tests)
  modified:
    - src/work_data_hub_pro/governance/evidence_index/file_store.py
    - src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py
    - src/work_data_hub_pro/apps/etl_cli/main.py

key-decisions:
  - "Used shared _resolve_package_path_for_run() in FileEvidenceIndex so gate_summary, checkpoint_results, publication_results, compatibility_case, and report all use the same fail-closed guard"
  - "Preserved _resolve_package_path() in diagnostics.py (called for evidence_paths construction) to match file_store enforcement"
  - "ValueError is caught at CLI boundary in replay_diagnose() before Typer's exception handler to ensure clean stderr output"

patterns-established:
  - "Pattern: manifest package_paths must resolve inside the comparison-run root; absolute paths and .. escapes raise ValueError"
  - "Pattern: invalid CLI identifiers surface as typed stderr messages, not Python tracebacks"

requirements-completed: [PIPE-03, OPS-01, PAR-02]

# Metrics
duration: ~3min
completed: 2026-04-13
---

# Phase 03.1 Plan 02: Diagnose Hardening Summary

**Fail-closed package-path enforcement and typed invalid-id CLI handling for replay diagnose**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-13T12:10 UTC
- **Completed:** 2026-04-13T12:13 UTC
- **Tasks:** 2 (TDD RED + GREEN)
- **Files modified:** 3 source, 1 test

## Accomplishments
- Manifest package_paths can no longer redirect diagnose reads outside the current comparison-run package
- `replay diagnose --comparison-run-id ../evil` returns typed stderr message and exit code 1 with no traceback
- Valid replay diagnose JSON output and missing-run behavior preserved

## Task Commits

Each task was committed atomically:

1. **Task 1 (TDD RED): Add failing contract tests** - `d76dcd8` (test)
2. **Task 2 (GREEN): Implement fail-closed path resolution and typed CLI handling** - `630ccc8` (feat)

## Files Created/Modified

- `src/work_data_hub_pro/governance/evidence_index/file_store.py` - Added `_resolve_package_path_for_run()` that rejects absolute and escaping paths inside the run root
- `src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py` - Updated `_resolve_package_path()` to accept `run_root`, raise ValueError for path escapes
- `src/work_data_hub_pro/apps/etl_cli/main.py` - Catch `ValueError` in `replay_diagnose()`, emit `Invalid comparison_run_id: {id}` to stderr with exit code 1
- `tests/contracts/test_replay_diagnose_contracts.py` - Extended with 3 new tests for path-escape rejection and typed CLI invalid-id error

## Decisions Made

- Used shared `_resolve_package_path_for_run()` in FileEvidenceIndex so gate_summary, checkpoint_results, publication_results, compatibility_case, and report all use the same fail-closed guard
- Preserved `_resolve_package_path()` in diagnostics.py (called for evidence_paths construction) to match file_store enforcement
- ValueError is caught at CLI boundary in `replay_diagnose()` before Typer's exception handler to ensure clean stderr output

## Deviations from Plan

None - plan executed exactly as written.

## Verification

All 16 tests pass:

```
uv run pytest tests/contracts/test_replay_diagnose_contracts.py tests/contracts/test_replay_cli_contracts.py tests/contracts/test_phase2_gate_contracts.py -v
============================== 16 passed in 0.42s ==============================
```

Key patterns verified present:
- `_resolve_package_path_for_run` in `file_store.py`
- `Invalid comparison_run_id` in `main.py`
- `package_paths` ValueError raises in both `diagnostics.py` and `file_store.py`

## Issues Encountered

None.

## Threat Surface

| Flag | File | Description |
|------|------|-------------|
| path_escape_rejected | file_store.py, diagnostics.py | Absolute and escaping relative manifest paths now raise ValueError |
| cli_no_traceback | main.py | Invalid comparison_run_id surfaces typed error, not Python traceback |

---
*Phase: 03.1-phase-3-governance-remediation-truthful-failure-evidence-and*
*Completed: 2026-04-13*
