---
phase: 06-minimum-viable-workbook-discovery-to-postgresql-publication
plan: 04
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
files_modified:
  - docs/runbooks/publish-annuity-performance.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VERIFICATION.md
autonomous: true
requirements:
  - RUN-01a
must_haves:
  truths:
    - "Phase 6 has an operator-usable runbook that matches the admitted DDL, env-var, CLI, verification, and teardown path."
    - "RUN-01a exists in REQUIREMENTS.md and is traceable to Phase 6 without falsely closing RUN-01."
    - "Roadmap and Phase 6 verification artifacts describe the actual plan set and PG acceptance truth gate."
  artifacts:
    - path: "docs/runbooks/publish-annuity-performance.md"
      provides: "Operator runbook for the PG publication pilot."
    - path: ".planning/REQUIREMENTS.md"
      provides: "RUN-01a requirement text and traceability row."
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 goal, requirement list, and concrete plan inventory."
    - path: ".planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VERIFICATION.md"
      provides: "Updated Phase 6 verification contract aligned to the implemented plan set."
  key_links:
    - from: "docs/runbooks/publish-annuity-performance.md"
      to: "src/work_data_hub_pro/apps/etl_cli/main.py"
      via: "documented publish command and expected stdout"
      pattern: "publish annuity-performance"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "RUN-01a traceability"
      pattern: "RUN-01a"
    - from: ".planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VERIFICATION.md"
      to: "tests/integration/test_postgres_publication_pilot.py"
      via: "phase truth gate command"
      pattern: "test_postgres_publication_pilot"
---

<objective>
Close the Phase 6 governance/documentation loop so the pilot is executable and traceable after code lands.

Purpose: satisfy D-16 and D-21 by documenting the real operator flow, updating requirement traceability, and aligning roadmap/verification artifacts to the concrete Phase 6 plan set.
Output: publish runbook, `RUN-01a` requirement entry, updated Phase 6 roadmap section, and synchronized `06-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VALIDATION.md
@docs/runbooks/annuity-performance-replay.md
@src/work_data_hub_pro/apps/etl_cli/main.py
@config/schemas/annuity_performance/fact_annuity_performance.sql
@config/schemas/annuity_performance/company_reference.sql
@config/schemas/annuity_performance/contract_state.sql
@config/schemas/annuity_performance/monthly_snapshot.sql
@tests/integration/test_postgres_publication_pilot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the operator runbook that exactly matches the admitted PG pilot</name>
  <read_first>
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
    @docs/runbooks/annuity-performance-replay.md
    @src/work_data_hub_pro/apps/etl_cli/main.py
    @config/schemas/annuity_performance/fact_annuity_performance.sql
    @config/schemas/annuity_performance/company_reference.sql
    @config/schemas/annuity_performance/contract_state.sql
    @config/schemas/annuity_performance/monthly_snapshot.sql
  </read_first>
  <files>docs/runbooks/publish-annuity-performance.md</files>
  <action>Create `docs/runbooks/publish-annuity-performance.md` using the existing concise runbook structure: goal, inputs, wrapper command, expected output, canonical workflow, agent CLI. Document the exact env vars from D-07 (`WDH_PG_DSN`, optional `DATABASE_URL`, optional `WDH_PG_SCHEMA`), the four DDL files from D-08, the explicit CLI command from D-13, `run_id`-first output from D-14, read-back verification, Docker/testcontainer expectations, and teardown. Keep the runbook narrowly scoped to annuity_performance and Phase 6; do not add tracing/evidence steps or other domains.</action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; text = Path('docs/runbooks/publish-annuity-performance.md').read_text(encoding='utf-8'); required = ['WDH_PG_DSN','publish annuity-performance','fact_annuity_performance.sql','company_reference.sql','contract_state.sql','monthly_snapshot.sql']; assert all(item in text for item in required); print('phase6-runbook-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The runbook names the exact env vars, DDL files, and CLI command admitted in CONTEXT.md.
    - The runbook includes setup, invocation, verification, and teardown steps.
    - The runbook does not instruct operators to use tracing/evidence files for the PG path.
  </acceptance_criteria>
  <done>Operators have one committed, phase-accurate guide for the PG publication pilot.</done>
</task>

<task type="auto">
  <name>Task 2: Add RUN-01a traceability and synchronize roadmap/verification artifacts</name>
  <read_first>
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
    @.planning/REQUIREMENTS.md
    @.planning/ROADMAP.md
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VALIDATION.md
    @tests/integration/test_postgres_publication_pilot.py
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VERIFICATION.md</files>
  <action>Append `RUN-01a` to `.planning/REQUIREMENTS.md` with the exact pilot-scoped definition from D-16 and add a traceability row `RUN-01a | Phase 6 | Pending`, while leaving `RUN-01` open. Update the Phase 6 section in `.planning/ROADMAP.md` from placeholders to the actual goal, requirement list (`RUN-01a` primary, with wording that RUN-01 remains open), plan count, and concrete plan list. Create or update `06-VERIFICATION.md` so its task map, wave map, and truth-gate commands match the implemented plan set, including the PG read-back diff command and the full-suite command. Do not claim this phase closes RUN-01.</action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; req = Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); roadmap = Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); verification = Path('.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VERIFICATION.md').read_text(encoding='utf-8'); assert 'RUN-01a' in req; assert 'RUN-01a' in roadmap; assert '06-01-PLAN.md' in roadmap and '06-04-PLAN.md' in roadmap; assert 'test_postgres_publication_pilot.py' in verification; print('phase6-governance-sync-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `RUN-01a` appears in REQUIREMENTS with a Phase 6 traceability row, and RUN-01 remains open.
    - The Phase 6 roadmap section no longer contains placeholders and lists the four real plan files.
    - `06-VERIFICATION.md` references the real PG pilot commands and current wave/task structure.
  </acceptance_criteria>
  <done>Phase 6 planning, requirement traceability, and verification docs all describe the same real pilot.</done>
</task>

<task type="auto">
  <name>Task 3: Run the explicit full-suite phase gate before any Phase 6 completion claim</name>
  <read_first>
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VALIDATION.md
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VERIFICATION.md
    @tests/integration/test_postgres_publication_pilot.py
  </read_first>
  <files>.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VERIFICATION.md</files>
  <action>Update `06-VERIFICATION.md` so the final phase gate explicitly requires `uv run pytest -v` before any Phase 6 completion claim, in addition to the PostgreSQL pilot truth-gate command. Record that this full-suite command is the mandatory final verification step required by repository CLAUDE instructions and implementation-verification discipline; do not treat the validation doc alone as sufficient coverage.</action>
  <verify>
    <automated>uv run pytest -v</automated>
  </verify>
  <acceptance_criteria>
    - `06-VERIFICATION.md` explicitly lists `uv run pytest -v` as the final phase-complete gate.
    - The final wave contains a direct full-suite verification task instead of only indirect references in validation docs.
  </acceptance_criteria>
  <done>The Phase 6 plan set contains an explicit repository-wide final verification step before completion claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator execution | misleading docs can cause schema drift, wrong env setup, or incorrect validation claims |
| planning docs -> requirement closure claims | roadmap/requirements text can incorrectly overstate what Phase 6 closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | R | runbook and verification docs | mitigate | document the exact env vars, DDL files, CLI command, and test commands used by the pilot so operator evidence is reproducible |
| T-06-13 | T | `.planning/REQUIREMENTS.md` | mitigate | add `RUN-01a` explicitly and leave `RUN-01` open so Phase 6 does not over-claim runtime expansion closure |
| T-06-14 | T | `.planning/ROADMAP.md` / `06-VERIFICATION.md` | mitigate | replace placeholders with actual Phase 6 plan and verification inventory tied to the PG read-back diff truth gate |
</threat_model>

<verification>
Use the embedded Python path checks above, then rely on the documented PG pilot test and `uv run pytest -v` for final slice verification.
</verification>

<success_criteria>
- A committed runbook exists for the Phase 6 PG pilot and matches the real command/schema path.
- `RUN-01a` is traceable in REQUIREMENTS and ROADMAP without falsely closing `RUN-01`.
- `06-VERIFICATION.md` is synchronized to the final Phase 6 plan set, PG acceptance gate, and explicit `uv run pytest -v` phase-complete gate.
</success_criteria>

<output>
After completion, create `.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-04-SUMMARY.md`
</output>
