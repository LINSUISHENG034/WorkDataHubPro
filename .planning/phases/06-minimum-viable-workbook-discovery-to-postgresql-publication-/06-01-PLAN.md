---
phase: 06-minimum-viable-workbook-discovery-to-postgresql-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/work_data_hub_pro/platform/storage/protocols.py
  - src/work_data_hub_pro/platform/storage/errors.py
  - src/work_data_hub_pro/platform/publication/service.py
  - src/work_data_hub_pro/capabilities/projections/contract_state.py
  - src/work_data_hub_pro/capabilities/projections/monthly_snapshot.py
  - tests/integration/test_publication_service.py
autonomous: true
requirements:
  - RUN-01a
must_haves:
  truths:
    - "PublicationService can execute against a storage contract instead of a concrete InMemoryTableStore type."
    - "The annuity_performance projection classes can consume the same minimal storage seam without changing projection business logic."
    - "Whole-run transaction wrapping is available when the injected store supports it and remains harmless for in-memory execution."
  artifacts:
    - path: "src/work_data_hub_pro/platform/storage/protocols.py"
      provides: "Single source of truth for the minimum table-store protocol."
      exports: ["TableStore"]
    - path: "src/work_data_hub_pro/platform/storage/errors.py"
      provides: "Typed PostgreSQL adapter error hierarchy."
      exports: ["PostgresAdapterError", "PostgresAdapterConfigError", "PostgresConnectionError", "PostgresSchemaMismatchError", "PostgresWriteError"]
    - path: "src/work_data_hub_pro/platform/publication/service.py"
      provides: "Storage-agnostic publication execution with optional transaction boundary."
      exports: ["PublicationService"]
  key_links:
    - from: "src/work_data_hub_pro/platform/publication/service.py"
      to: "src/work_data_hub_pro/platform/storage/protocols.py"
      via: "constructor annotation and transaction hook"
      pattern: "TableStore|transaction"
    - from: "src/work_data_hub_pro/capabilities/projections/contract_state.py"
      to: "src/work_data_hub_pro/platform/storage/protocols.py"
      via: "constructor storage annotation"
      pattern: "TableStore"
    - from: "src/work_data_hub_pro/capabilities/projections/monthly_snapshot.py"
      to: "src/work_data_hub_pro/platform/storage/protocols.py"
      via: "constructor storage annotation"
      pattern: "TableStore"
---

<objective>
Create the narrow storage seam that Phase 6 needs before any PostgreSQL-specific code lands.

Purpose: keep the pilot in the publication/storage boundary while honoring D-03, D-06, D-10, and the explicit hidden seam called out for `ContractStateProjection` and `MonthlySnapshotProjection`.
Output: one minimal table-store protocol, one typed PostgreSQL error module, storage-agnostic publication/projection typing, and regression coverage proving the in-memory path still behaves the same.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-RESEARCH.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-PATTERNS.md
@src/work_data_hub_pro/platform/storage/in_memory_tables.py
@src/work_data_hub_pro/platform/publication/service.py
@src/work_data_hub_pro/capabilities/projections/contract_state.py
@src/work_data_hub_pro/capabilities/projections/monthly_snapshot.py
@tests/integration/test_publication_service.py

<interfaces>
From `src/work_data_hub_pro/platform/storage/in_memory_tables.py`:
```python
class InMemoryTableStore:
    def refresh(self, target_name: str, rows: list[dict[str, object]]) -> int: ...
    def upsert(self, target_name: str, rows: list[dict[str, object]], *, key_fields: list[str]) -> int: ...
    def append(self, target_name: str, rows: list[dict[str, object]]) -> int: ...
    def read(self, target_name: str) -> list[dict[str, object]]: ...
```

From `src/work_data_hub_pro/platform/publication/service.py`:
```python
class PublicationService:
    def __init__(self, storage: InMemoryTableStore) -> None: ...
    def execute(self, bundles: list[PublicationBundle]) -> list[PublicationResult]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the minimum table-store protocol and typed PostgreSQL error surface</name>
  <read_first>
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-PATTERNS.md
    @src/work_data_hub_pro/platform/storage/in_memory_tables.py
    @src/work_data_hub_pro/platform/publication/service.py
  </read_first>
  <files>src/work_data_hub_pro/platform/storage/protocols.py, src/work_data_hub_pro/platform/storage/errors.py</files>
  <behavior>
    - Test 1: importing `TableStore` succeeds and exposes `refresh`, `upsert`, `append`, `read`, and `transaction` as the only required Phase 6 seam per D-06.
    - Test 2: importing `PostgresAdapterConfigError`, `PostgresConnectionError`, `PostgresSchemaMismatchError`, and `PostgresWriteError` succeeds from one module per D-11.
  </behavior>
  <action>Create `TableStore` in `platform/storage/protocols.py` as the single minimum Protocol used by this phase; include `refresh`, `upsert`, `append`, `read`, and a transaction hook that `PublicationService` can use without introducing any factory, registry, or generic storage layer per D-06. Create `platform/storage/errors.py` exporting exactly `PostgresAdapterError`, `PostgresAdapterConfigError`, `PostgresConnectionError`, `PostgresSchemaMismatchError`, and `PostgresWriteError` per D-11. Keep constructors explicit and small; do not add extra adapter classes, retry helpers, or shared registries.</action>
  <verify>
    <automated>uv run python -c "from work_data_hub_pro.platform.storage.protocols import TableStore; from work_data_hub_pro.platform.storage.errors import PostgresAdapterConfigError, PostgresConnectionError, PostgresSchemaMismatchError, PostgresWriteError; print('phase6-storage-seam-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/work_data_hub_pro/platform/storage/protocols.py` exists and exports exactly one minimum store protocol for Phase 6.
    - `src/work_data_hub_pro/platform/storage/errors.py` exists and exports the five typed PostgreSQL errors named in D-11.
    - No factory, registry, plugin system, or generic storage abstraction beyond the minimum protocol appears in these files.
  </acceptance_criteria>
  <done>The storage seam and typed PG error names are importable and ready for downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make publication and projection consumers use the seam without changing business semantics</name>
  <read_first>
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
    @src/work_data_hub_pro/platform/storage/protocols.py
    @src/work_data_hub_pro/platform/publication/service.py
    @src/work_data_hub_pro/capabilities/projections/contract_state.py
    @src/work_data_hub_pro/capabilities/projections/monthly_snapshot.py
  </read_first>
  <files>src/work_data_hub_pro/platform/publication/service.py, src/work_data_hub_pro/capabilities/projections/contract_state.py, src/work_data_hub_pro/capabilities/projections/monthly_snapshot.py</files>
  <behavior>
    - Test 1: `PublicationService` still dispatches `REFRESH`, `UPSERT`, and `APPEND_ONLY` exactly as before while accepting any `TableStore` implementation per D-06.
    - Test 2: when the injected store exposes `transaction()`, the whole execute loop runs inside one transaction boundary per D-10.
    - Test 3: `ContractStateProjection` and `MonthlySnapshotProjection` accept the seam type while keeping their row-shaping logic byte-identical, addressing the hidden seam called out in planning guidance.
  </behavior>
  <action>Change constructor annotations in `PublicationService`, `ContractStateProjection`, and `MonthlySnapshotProjection` from `InMemoryTableStore` to the new protocol; do not move or rewrite projection business logic per D-03. In `PublicationService.execute`, wrap the full bundle loop in one transaction context only when the injected store supports it per D-10; keep the existing mode dispatch and fail-fast `PublicationExecutionError` wrapping intact. Do not add per-target commits, store factories, or publication-plan refactors.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_publication_service.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `PublicationService.__init__` no longer hard-types `InMemoryTableStore`.
    - `PublicationService.execute` retains the existing mode dispatch and `PublicationExecutionError` wrapper while adding a whole-run transaction seam.
    - `contract_state.py` and `monthly_snapshot.py` only change storage typing/imports; their business row logic remains unchanged.
  </acceptance_criteria>
  <done>The PG path can be injected later without semantic drift in publication or projections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock regression coverage around the seam so the in-memory path stays valid</name>
  <read_first>
    @tests/integration/test_publication_service.py
    @src/work_data_hub_pro/platform/publication/service.py
    @src/work_data_hub_pro/platform/storage/protocols.py
    @src/work_data_hub_pro/platform/storage/in_memory_tables.py
  </read_first>
  <files>tests/integration/test_publication_service.py</files>
  <behavior>
    - Test 1: the existing in-memory publication flow still succeeds with the new seam.
    - Test 2: a store with a transaction hook records one outer transaction boundary for the whole execute call per D-10.
    - Test 3: write failures still surface as `PublicationExecutionError` with the offending publication id and target name.
  </behavior>
  <action>Extend `tests/integration/test_publication_service.py` with narrow regression cases that prove the seam did not break current behavior and that whole-run transaction wrapping occurs only at the outer execute boundary. Reuse the existing direct pytest style from Phase 5; do not create a new test harness or duplicate the future PostgreSQL integration suite.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_publication_service.py -v</automated>
  </verify>
  <acceptance_criteria>
    - The integration module contains explicit assertions for seam compatibility and transaction-hook behavior.
    - Existing in-memory happy-path assertions still pass unchanged.
    - Failure assertions still check typed `PublicationExecutionError` fields instead of generic booleans.
  </acceptance_criteria>
  <done>The seam is protected by targeted regression tests before PostgreSQL code lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publication service -> storage adapter | untrusted adapter behavior crosses into write execution and rollback semantics |
| projections -> storage read surface | projection consumers rely on stable table reads without inheriting PG-specific logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `platform/publication/service.py` | mitigate | keep one minimal protocol and preserve existing fail-fast wrapper so storage injection cannot bypass typed execution errors |
| T-06-02 | T | `platform/publication/service.py` transaction seam | mitigate | add one whole-run transaction hook per D-10; integration tests must prove the outer transaction boundary is used |
| T-06-03 | E | projection constructors | accept | constructor typing broadens but business logic stays unchanged; no new projection authority is added |
</threat_model>

<verification>
Run `uv run pytest tests/integration/test_publication_service.py -v` after the plan, then keep this module green while downstream PG plans land.
</verification>

<success_criteria>
- Storage protocol and typed PG errors are importable from stable module paths.
- `PublicationService` and both projection classes use the new seam without business-logic drift.
- Existing publication-service integration coverage proves in-memory compatibility and outer transaction-hook behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-01-SUMMARY.md`
</output>
