---
phase: 06-minimum-viable-workbook-discovery-to-postgresql-publication
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/work_data_hub_pro/apps/etl_cli/main.py
  - tests/integration/test_postgres_publication_pilot.py
autonomous: true
requirements:
  - RUN-01a
must_haves:
  truths:
    - "Operators can run `publish annuity-performance <workbook> <period>` and get a generated run_id printed first on stdout."
    - "The publish command writes all four annuity_performance targets into PostgreSQL under one transaction and does not write evidence/tracing files."
    - "PG rows read back from the four target tables match the replay output for the same workbook and period, with deterministic normalization for append-only ordering."
    - "A seeded mid-run failure rolls back the whole run so no target rows remain."
  artifacts:
    - path: "src/work_data_hub_pro/apps/etl_cli/main.py"
      provides: "Mounted publish Typer subgroup and annuity-performance command."
    - path: "tests/integration/test_postgres_publication_pilot.py"
      provides: "End-to-end PG publication pilot truth gate and rollback coverage."
  key_links:
    - from: "src/work_data_hub_pro/apps/etl_cli/main.py"
      to: "src/work_data_hub_pro/platform/storage/postgres_tables.py"
      via: "publish command creates PostgresTableStore from env and executes PublicationService"
      pattern: "publish|PostgresTableStore"
    - from: "tests/integration/test_postgres_publication_pilot.py"
      to: "tests/replay/test_annuity_performance_slice.py"
      via: "replay output as expected truth source"
      pattern: "annuity_performance|sorted"
    - from: "tests/integration/test_postgres_publication_pilot.py"
      to: "src/work_data_hub_pro/apps/etl_cli/main.py"
      via: "CLI invocation and run_id-first stdout assertions"
      pattern: "publish annuity-performance|run_id"
---

<objective>
Wire the operator-visible publish command and the one true end-to-end acceptance suite for the PostgreSQL pilot.

Purpose: satisfy D-12 through D-20 by making the pilot executable from the CLI and proving the PG path against replay truth, transaction rollback, and operator-visible run identifiers.
Output: mounted `publish` CLI subgroup, `publish annuity-performance` command, a Docker-aware PostgreSQL integration suite, and rollback/read-back parity coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-RESEARCH.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VALIDATION.md
@.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-PATTERNS.md
@src/work_data_hub_pro/apps/etl_cli/main.py
@src/work_data_hub_pro/platform/publication/service.py
@src/work_data_hub_pro/platform/storage/postgres_tables.py
@config/policies/publication.json
@tests/integration/test_publication_service.py
@tests/replay/test_annuity_performance_slice.py

<interfaces>
From Phase 6 decisions:
```text
CLI: publish annuity-performance <workbook> <period>
run_id = f"pub-{domain}-{period}-{utc_iso_timestamp}"
run_id prints first on successful stdout
```

From `PublicationService` contract:
```python
service.execute(bundles: list[PublicationBundle]) -> list[PublicationResult]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount the publish CLI subgroup and implement the annuity-performance command</name>
  <read_first>
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-PATTERNS.md
    @src/work_data_hub_pro/apps/etl_cli/main.py
    @src/work_data_hub_pro/platform/publication/service.py
    @src/work_data_hub_pro/platform/storage/postgres_tables.py
  </read_first>
  <files>src/work_data_hub_pro/apps/etl_cli/main.py</files>
  <behavior>
    - Test 1: `publish_app` is mounted via `app.add_typer(..., name='publish')` and exposes `annuity-performance` per D-13.
    - Test 2: the command accepts explicit positional `<workbook>` and `<period>` arguments only, generates `run_id`, `batch_id`, and `publication_id` values internally per D-12 and D-14.
    - Test 3: successful stdout prints `run_id=...` as the first stable line and then emits the rest of the operator-visible summary.
    - Test 4: the command writes only to PostgreSQL and does not write evidence index or tracing artifacts per D-15.
  </behavior>
  <action>Extend `src/work_data_hub_pro/apps/etl_cli/main.py` with `publish_app` and a `publish annuity-performance` command that mirrors the existing Typer subgroup style per D-13. Feed the explicit workbook path into the existing annuity pipeline unchanged per D-03, generate identifiers inside the command per D-14, build the four publication bundles from the existing policy, inject `PostgresTableStore`, and execute through `PublicationService`. Keep the command read-only against the workbook and write-only against PG per D-15. Do not add directory scanning, `--run-id`, manifest files, tracing/evidence persistence, or generic CLI scaffolding.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_postgres_publication_pilot.py -v -k "publish_cli_contracts or run_id_first_output"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/etl_cli/main.py` contains a mounted `publish` subgroup and an `annuity-performance` command.
    - The command signature uses positional workbook and period arguments; no discovery flags or run-id override are added.
    - A targeted CLI contract test proves the command generates `run_id`, `batch_id`, and `publication_id` inside the command, prints `run_id=` first on successful output, and routes publication through the existing publication service instead of direct SQL in the CLI.
  </acceptance_criteria>
  <done>The operator-visible PG publish entrypoint exists and stays within execution-adapter responsibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the PostgreSQL pilot integration suite as the sole acceptance truth gate</name>
  <read_first>
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-CONTEXT.md
    @.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-VALIDATION.md
    @src/work_data_hub_pro/apps/etl_cli/main.py
    @src/work_data_hub_pro/platform/storage/postgres_tables.py
    @tests/replay/test_annuity_performance_slice.py
    @tests/integration/test_publication_service.py
  </read_first>
  <files>tests/integration/test_postgres_publication_pilot.py</files>
  <behavior>
    - Test 1: with Docker available, the testcontainer-backed suite runs `publish annuity-performance` for one real workbook+period and verifies all four target tables match replay output per D-17 and D-18.
    - Test 2: the suite marks Docker-dependent cases with `@pytest.mark.postgres` and cleanly skips when Docker is unavailable per D-19.
    - Test 3: the CLI stdout assertion checks `run_id` is the first successful line per D-14.
    - Test 4: a seeded mid-run failure rolls back the entire run and leaves all four tables empty per D-10 and D-20.
  </behavior>
  <action>Create `tests/integration/test_postgres_publication_pilot.py` as the one end-to-end pilot truth gate. This module must also contain targeted CLI contract coverage for Task 1: subgroup mounting, positional `<workbook> <period>` arguments, internally generated `run_id` / `batch_id` / `publication_id`, and `run_id=` as the first successful stdout line. Reuse replay output for the same workbook and period as the expected dataset per D-17 and D-18; read back PG rows using `PostgresTableStore.read()` for `fact_annuity_performance`, `company_reference`, `contract_state`, and `monthly_snapshot`, then compare deterministically, including a documented stable sort for `APPEND_ONLY`. Add a module-scoped PostgreSQL container fixture and `postgres` marker per D-19, and make Docker-unavailable cases skip cleanly from that fixture. Add explicit rollback coverage that seeds a mid-run target failure and asserts all four tables are empty afterward per D-20. Do not create a second business-semantic harness or compare PG output against new bespoke expected fixtures.</action>
  <verify>
    <automated>uv run pytest tests/integration/test_postgres_publication_pilot.py -v -m postgres</automated>
  </verify>
  <acceptance_criteria>
    - The integration module contains Docker-aware `postgres`-marked tests for happy path and rollback path.
    - Happy-path assertions compare PG read-back rows to replay output for all four locked targets.
    - Rollback assertions explicitly prove whole-run atomicity by checking all four tables are empty after the seeded failure.
  </acceptance_criteria>
  <done>The pilot has one operator-visible CLI and one authoritative PG-vs-replay acceptance suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args -> publish command | workbook path and period are untrusted inputs entering the execution adapter |
| publish command -> publication/storage runtime | command orchestration can accidentally bypass policy, transaction, or traceability rules |
| replay truth -> PG read-back diff | acceptance can be weakened if expected rows stop coming from replay output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | T | `apps/etl_cli/main.py` | mitigate | keep CLI orchestration limited to explicit workbook/period inputs and route all writes through `PublicationService` + `PostgresTableStore` |
| T-06-09 | R | publish command stdout correlation | mitigate | print generated `run_id` first on successful stdout per D-14 so operators can tie CLI runs to PG rows |
| T-06-10 | T | end-to-end acceptance suite | mitigate | require replay output as the expected truth source per D-17/D-18; do not add a second semantic harness |
| T-06-11 | D | whole-run rollback | mitigate | seeded failure test must prove all four tables remain empty after rollback per D-10 and D-20 |
</threat_model>

<verification>
Run `uv run pytest tests/integration/test_postgres_publication_pilot.py -v -m postgres` in a Docker-available environment, and keep rollback + read-back parity assertions green.
</verification>

<success_criteria>
- `publish annuity-performance <workbook> <period>` is mounted and operator-visible.
- The command generates IDs internally, prints `run_id` first, and writes only to PostgreSQL.
- The PG pilot integration suite proves all four target writes, replay read-back equality, Docker-aware skipping, and whole-run rollback.
</success_criteria>

<output>
After completion, create `.planning/phases/06-minimum-viable-workbook-discovery-to-postgresql-publication-/06-03-SUMMARY.md`
</output>
