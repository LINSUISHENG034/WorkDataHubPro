---
phase: 01-legacy-capability-mapping-parity-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv
  - .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/intake-path-map.csv
  - .planning/phases/01-legacy-capability-mapping-parity-harness/schema/capability-map.schema.md
  - tests/contracts/test_phase1_mapping_artifacts.py
autonomous: true
requirements:
  - MAP-01
  - MAP-02
must_haves:
  truths:
    - "Reviewers can identify each parity-critical legacy capability and its Pro owner/status from one authoritative matrix."
    - "Reviewers can trace critical source-recognition paths from legacy to explicit Pro intake contracts."
    - "Mapping rows include ambiguity notes so unresolved parity risks are visible before implementation phases."
  artifacts:
    - path: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv"
      provides: "Dual-layer business capability to stage-chain mapping."
      contains: "capability_id,domain,business_capability,legacy_owner_path,legacy_stage_chain,pro_owner_path,pro_stage_chain,migration_status,parity_criticality,ambiguity_notes"
    - path: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/intake-path-map.csv"
      provides: "Critical source-recognition and intake validation mapping."
      contains: "legacy_source_path,legacy_recognition_rule,pro_intake_contract,validation_check,test_anchor,status"
    - path: "tests/contracts/test_phase1_mapping_artifacts.py"
      provides: "Contract checks for required columns and minimum coverage rows."
      contains: "test_capability_map_required_columns,test_intake_map_required_columns"
  key_links:
    - from: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv"
      to: "src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py"
      via: "pro_owner_path and pro_stage_chain values"
      pattern: "annuity_performance_slice.py"
    - from: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/intake-path-map.csv"
      to: "tests/integration/test_*_intake.py"
      via: "validation_check and test_anchor columns"
      pattern: "test_.*_intake.py"
---

<objective>
Create the authoritative mapping baseline for Phase 1 by delivering dual-layer capability and intake-path artifacts plus contract tests.

Purpose: satisfy MAP-01 and MAP-02 without scope creep into refactor or CI hard-gating.
Output: stable schema + artifact files that later parity and rule-classification plans can consume.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-legacy-capability-mapping-parity-harness/01-CONTEXT.md
@.planning/phases/01-legacy-capability-mapping-parity-harness/01-RESEARCH.md
@docs/gsd/discuss/2026-04-12-phase-1-gray-area-recommendations.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md

<interfaces>
From `src/work_data_hub_pro/apps/etl_cli/main.py`:
- replay CLI owns entrypoint paths for `annuity_performance`, `annual_award`, `annual_loss` (mapping target for `pro_owner_path`).

From `src/work_data_hub_pro/apps/orchestration/replay/*.py`:
- `run_annuity_performance_slice(...)`
- `run_annual_award_slice(...)`
- `run_annual_loss_slice(...)`
These are canonical Phase 1 mapping endpoints for stage-chain evidence (per D-01, D-03, D-04).

From `src/work_data_hub_pro/capabilities/source_intake/*/service.py`:
- intake contract ownership must be referenced from `intake-path-map.csv` as explicit `pro_intake_contract` rows (per D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define mapping schemas and required columns (D-01, D-02, D-06)</name>
  <files>.planning/phases/01-legacy-capability-mapping-parity-harness/schema/capability-map.schema.md</files>
  <read_first>
    .planning/phases/01-legacy-capability-mapping-parity-harness/01-CONTEXT.md
    docs/gsd/discuss/2026-04-12-phase-1-gray-area-recommendations.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>Create schema definitions for `capability-map.csv` and `intake-path-map.csv` with exact required columns from D-01/D-02 and D-06. Include required `legacy_stage_chain` and `pro_stage_chain` columns for the dual-layer mapping model, allowed enums for `migration_status` and `parity_criticality`, and a rule that `ambiguity_notes` cannot be omitted (empty string allowed).</action>
  <acceptance_criteria>Run `rg -n \"capability_id|legacy_owner_path|legacy_stage_chain|pro_owner_path|pro_stage_chain|migration_status|parity_criticality|ambiguity_notes|legacy_recognition_rule|pro_intake_contract|validation_check|test_anchor\" .planning/phases/01-legacy-capability-mapping-parity-harness/schema/capability-map.schema.md` and confirm all required fields are present.</acceptance_criteria>
  <verify>
    <automated>rg -n "capability_id|legacy_owner_path|legacy_stage_chain|pro_owner_path|pro_stage_chain|migration_status|parity_criticality|ambiguity_notes|legacy_recognition_rule|pro_intake_contract|validation_check|test_anchor" .planning/phases/01-legacy-capability-mapping-parity-harness/schema/capability-map.schema.md</automated>
  </verify>
  <done>Schema file defines both artifact contracts and includes D-01/D-02/D-06 required fields, including the stage-chain traceability columns required by the dual-layer mapping model.</done>
</task>

<task type="auto">
  <name>Task 2: Build capability and intake-path mapping artifacts (D-03, D-04)</name>
  <files>.planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv, .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/intake-path-map.csv</files>
  <read_first>
    .planning/phases/01-legacy-capability-mapping-parity-harness/schema/capability-map.schema.md
    docs/superpowers/specs/2026-04-11-workdatahubpro-first-wave-legacy-coverage-matrix.md
    .planning/codebase/ARCHITECTURE.md
  </read_first>
  <action>Create artifact rows that deep-map `annuity_performance` and register breadth rows for `annual_award` and `annual_loss` (without equal-depth decomposition), exactly per D-03 and D-04. Ensure every capability row includes `legacy_stage_chain` and `pro_stage_chain`, migration status, parity criticality, and ambiguity notes; no placeholder labels like \"TBD\" without rationale.</action>
  <acceptance_criteria>Run `rg -n \"annuity_performance|annual_award|annual_loss\" .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv` and confirm all three domains exist with deep/breadth intent reflected in notes.</acceptance_criteria>
  <verify>
    <automated>rg -n "annuity_performance|annual_award|annual_loss" .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv</automated>
  </verify>
  <done>`capability-map.csv` and `intake-path-map.csv` exist, conform to schema columns, and include required Phase 1 domain coverage strategy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add contract tests for mapping artifact integrity</name>
  <files>tests/contracts/test_phase1_mapping_artifacts.py</files>
  <read_first>
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/intake-path-map.csv
    .planning/phases/01-legacy-capability-mapping-parity-harness/01-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: mapping artifacts must contain all required schema columns.
    - Test 2: `capability-map.csv` must include all three Phase 1 domains.
    - Test 3: deep-sample `annuity_performance` capability rows must include non-empty `legacy_stage_chain` and `pro_stage_chain` values.
    - Test 4: intake mapping rows must include non-empty validation check and test anchor fields.
  </behavior>
  <action>Write deterministic contract tests that parse the CSV artifacts and assert required columns, domain coverage, and non-empty traceability fields. Keep tests fast and local; do not invoke replay execution in this plan.</action>
  <acceptance_criteria>Run `uv run pytest tests/contracts/test_phase1_mapping_artifacts.py -v` and confirm all tests pass.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_phase1_mapping_artifacts.py -v</automated>
  </verify>
  <done>Contract tests pass and enforce MAP-01/MAP-02 artifact structure and minimum coverage in an automated way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy docs/assets -> Phase 1 mapping artifacts | Untrusted interpretation can introduce mapping tampering or omission. |
| mapping artifacts -> tests/contracts | Invalid artifact shape can silently bypass parity governance if not asserted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | capability-map.csv | mitigate | Enforce required columns and domain coverage in contract tests; reject incomplete rows. |
| T-01-02 | R | intake-path-map.csv | mitigate | Require explicit `legacy_owner_path`, `pro_intake_contract`, and `test_anchor` fields for auditability. |
| T-01-03 | I | mapping artifacts in `.planning` | accept | Phase 1 artifacts are local planning outputs (`commit_docs=false`); no secret payloads should be included. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_phase1_mapping_artifacts.py -v`
- `uv run pytest tests/contracts -v`
</verification>

<success_criteria>
- MAP-01 and MAP-02 are covered by artifact schema plus tested mapping artifacts.
- Mapping data includes D-03 deep sample and D-04 breadth registration.
- Artifact contracts are machine-validated, not manual-only.
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-capability-mapping-parity-harness/01-01-SUMMARY.md`
</output>
