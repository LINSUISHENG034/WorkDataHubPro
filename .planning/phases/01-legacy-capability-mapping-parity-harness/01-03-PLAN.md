---
phase: 01-legacy-capability-mapping-parity-harness
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json
  - .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json
  - .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md
  - .planning/phases/01-legacy-capability-mapping-parity-harness/schema/parity-baseline.schema.md
  - tests/contracts/test_phase1_parity_baseline_artifacts.py
autonomous: false
requirements:
  - PAR-01
must_haves:
  truths:
    - "Phase 1 has a reproducible parity baseline identity for annuity performance, annual award, and annual loss."
    - "Mismatch report output is populated from at least one real parity comparison run and includes severity-table-ready fields for offline adjudication."
    - "Human confirmation checkpoint has a stable decision log format with decision owner identity."
  artifacts:
    - path: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json"
      provides: "Dataset and golden-output identity contract for PAR-01 domains."
      contains: "domain,sample_batch_id,baseline_version,comparison_run_id"
    - path: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json"
      provides: "Parity summary and mismatch severity table payload."
      contains: "parity_summary,mismatch_table,severity,classification_reason"
    - path: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md"
      provides: "Offline human-confirmed gate decision record."
      contains: "decision_owner,decision,scope,follow_up"
    - path: "tests/contracts/test_phase1_parity_baseline_artifacts.py"
      provides: "Contract checks for baseline identity and checkpoint output shape."
      contains: "test_identity_fields_present,test_checkpoint_outputs_present"
  key_links:
    - from: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json"
      to: "reference/historical_replays/"
      via: "baseline fixture and snapshot references"
      pattern: "legacy_monthly_snapshot_2026_03.json"
    - from: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json"
      to: ".planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md"
      via: "comparison_run_id and decision owner traceability"
      pattern: "comparison_run_id|decision_owner"
---

<objective>
Define reproducible parity baseline and offline must-pass checkpoint assets for PAR-01, including required identity fields and human review gate format.

Purpose: establish a deterministic parity harness foundation for later CI gating phases.
Output: baseline schema + real-run-backed baseline/report/log artifacts + contract tests + human verification gate.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-legacy-capability-mapping-parity-harness/01-CONTEXT.md
@.planning/phases/01-legacy-capability-mapping-parity-harness/01-RESEARCH.md
@.planning/phases/01-legacy-capability-mapping-parity-harness/01-VALIDATION.md
@docs/gsd/discuss/2026-04-12-phase-1-gray-area-recommendations.md
@reference/historical_replays/

<interfaces>
From `reference/historical_replays/annuity_performance/legacy_monthly_snapshot_2026_03.json`:
- Snapshot identity and expected-output references anchor PAR-01 baseline for deep sample domain.

From `src/work_data_hub_pro/apps/orchestration/replay/*.py` outcomes:
- Replay outcome fields and evidence paths are source anchors for `comparison_run_id` and mismatch-summary linkage.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define baseline schema and baseline/decision-log artifact contracts (D-05, D-06, D-11 to D-15)</name>
  <files>.planning/phases/01-legacy-capability-mapping-parity-harness/schema/parity-baseline.schema.md, .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json, .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md</files>
  <read_first>
    .planning/phases/01-legacy-capability-mapping-parity-harness/01-CONTEXT.md
    .planning/phases/01-legacy-capability-mapping-parity-harness/01-RESEARCH.md
    .planning/phases/01-legacy-capability-mapping-parity-harness/01-VALIDATION.md
    reference/historical_replays/
    docs/gsd/discuss/2026-04-12-phase-1-gray-area-recommendations.md
  </read_first>
  <action>Create `parity-baseline.schema.md` first, requiring identity fields `domain`, `sample_batch_id`, `baseline_version`, `comparison_run_id`, and parity-critical intermediate checkpoints from D-06. Then create `parity-baseline.json` and `decision-log.md`, covering all PAR-01 domains with deep sample metadata for `annuity_performance` and breadth registration for `annual_award`/`annual_loss`. Include D-12 offline gate outputs and minimum evidence set from D-13 while explicitly not defining the deferred full evidence taxonomy (D-14).</action>
  <acceptance_criteria>Run `rg -n \"annuity_performance|annual_award|annual_loss|mapping completeness status|baseline dataset identity|human decision log\" .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md` and confirm required outputs are represented.</acceptance_criteria>
  <verify>
    <automated>rg -n "domain|sample_batch_id|baseline_version|comparison_run_id|source_recognition|canonical_fact_shape|identity_resolution_category|publication_key_fields|annuity_performance|annual_award|annual_loss|mapping completeness status|baseline dataset identity|human decision log" .planning/phases/01-legacy-capability-mapping-parity-harness/schema/parity-baseline.schema.md .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md</automated>
  </verify>
  <done>Phase 1 baseline and decision-log artifact contracts exist, include required checkpoint outputs, and remain within offline human-review scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute one real deep-sample parity comparison run and record mismatch report (D-03, D-05, D-06, D-12)</name>
  <files>.planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json</files>
  <read_first>
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/severity-policy.md
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/rule-classification.csv
    reference/historical_replays/annuity_performance/
  </read_first>
  <behavior>
    - Compare the deep-sample `annuity_performance` baseline against a real replay execution outcome, not a hand-written placeholder.
    - Populate `mismatch-report.json` with `comparison_run_id`, `parity_summary`, `mismatch_table`, `severity`, and `classification_reason`.
    - Record breadth domains `annual_award` and `annual_loss` as registered baseline scope when they are not the executed deep-sample comparison.
  </behavior>
  <action>Use the existing replay harness for `annuity_performance` to drive one deterministic parity comparison run and write the observed comparison result into `mismatch-report.json`. The artifact must distinguish actual compared domain output from breadth-only registered domains so the parity harness is evidence-backed rather than schema-only.</action>
  <acceptance_criteria>Run `uv run pytest tests/replay/test_annuity_performance_slice.py -v` and confirm pass, then run `rg -n \"comparison_run_id|annuity_performance|parity_summary|mismatch_table|severity|classification_reason\" .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json` and confirm the report is populated from a real run.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_annuity_performance_slice.py -v</automated>
    <automated>rg -n "comparison_run_id|annuity_performance|parity_summary|mismatch_table|severity|classification_reason" .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json</automated>
  </verify>
  <done>`mismatch-report.json` is backed by at least one real replay comparison run for the deep-sample domain and is not merely a template artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add contract tests for parity baseline/checkpoint artifact integrity</name>
  <files>tests/contracts/test_phase1_parity_baseline_artifacts.py</files>
  <read_first>
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md
  </read_first>
  <behavior>
    - Test 1: PAR-01 domains must all exist in baseline identity data.
    - Test 2: mismatch report must include severity table and parity summary keys.
    - Test 3: mismatch report must include at least one executed deep-sample comparison entry for `annuity_performance`.
    - Test 4: decision log template must include decision owner and explicit decision outcome fields.
  </behavior>
  <action>Write fast contract tests validating artifact key presence, domain coverage, deep-sample execution evidence, and required identity fields. Keep scope to artifact integrity checks only; replay correctness remains covered by the targeted replay command in Task 2.</action>
  <acceptance_criteria>Run `uv run pytest tests/contracts/test_phase1_parity_baseline_artifacts.py -v` and confirm pass.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_phase1_parity_baseline_artifacts.py -v</automated>
  </verify>
  <done>Automated tests enforce PAR-01 baseline and checkpoint artifact shape required for Phase 1 completion evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Offline must-pass checkpoint confirmation (D-11, D-12)</name>
  <files>.planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md</files>
  <read_first>
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/capability-map.csv
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/parity-baseline.json
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json
    .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md
  </read_first>
  <action>Perform the Phase 1 offline report gate review and record human decision in `decision-log.md`. This is a human-confirmed checkpoint, not CI hard-fail. Verify mapping completeness, baseline identity, parity summary, mismatch severity table, and decision ownership fields are all present.</action>
  <acceptance_criteria>Gate passes only if reviewer records `approved` or `approved-with-warn` with `decision_owner` and `comparison_run_id` in `decision-log.md`; otherwise execution remains blocked.</acceptance_criteria>
  <how-to-verify>
    1. Open the mapping, baseline, mismatch, and decision-log artifacts and confirm D-12 outputs are present.
    2. Confirm `comparison_run_id` matches between mismatch report and decision log.
    3. Add reviewer decision line in `decision-log.md` with owner/date/outcome.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or provide required corrections.</resume-signal>
  <verify>
    <automated>rg -n "decision_owner|comparison_run_id|approved|approved-with-warn|mismatch severity table|parity summary" .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json</automated>
  </verify>
  <done>Human-confirmed offline gate is logged with stable identifiers and explicit decision outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline fixture references -> parity-baseline.json | Incorrect or non-deterministic baseline identity can invalidate parity decisions. |
| mismatch report -> human decision log | Missing linkage can enable repudiation and weak audit trail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | parity-baseline.json | mitigate | Require deterministic identity fields and PAR-01 domain coverage via contract tests. |
| T-01-08 | R | decision-log.md | mitigate | Require decision owner + comparison run linkage and explicit outcome status in checkpoint task. |
| T-01-09 | D | offline checkpoint process | accept | Human-confirmed gate can pause progress by design in Phase 1; CI hard-fail is intentionally deferred. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_phase1_parity_baseline_artifacts.py -v`
- `uv run pytest tests/replay/test_annuity_performance_slice.py -v`
- `uv run pytest tests/contracts -v`
- `uv run pytest -v` (phase-close full-suite verification gate before completion sign-off)
- `rg -n "decision_owner|comparison_run_id" .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/decision-log.md .planning/phases/01-legacy-capability-mapping-parity-harness/artifacts/mismatch-report.json`
</verification>

<success_criteria>
- PAR-01 baseline and mismatch artifacts are reproducible with stable identity fields.
- At least one real deep-sample parity comparison run is captured before human checkpoint review.
- Offline report gate is human-confirmed and logged per D-11/D-12.
- Deferred scope is respected: no CI parity hard-fail promotion and no full evidence-directory standardization.
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-capability-mapping-parity-harness/01-03-SUMMARY.md`
</output>
