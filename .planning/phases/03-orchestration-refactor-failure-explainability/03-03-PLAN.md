---
phase: 03-orchestration-refactor-failure-explainability
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/work_data_hub_pro/apps/orchestration/replay/runtime.py
  - src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py
  - tests/integration/test_replay_setup_failures.py
  - tests/replay/test_annuity_performance_slice.py
  - tests/replay/test_phase2_annuity_performance_gates.py
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
must_haves:
  truths:
    - "The annuity replay runner uses shared replay primitives for common mechanics without hiding annuity-specific business wiring."
    - "Completed annuity runs expose a typed replay run report with a `primary_failure` only when checkpoint evaluation actually runs."
    - "Annuity setup/config failures raise typed replay setup exceptions instead of leaking raw validator and config errors."
  artifacts:
    - path: "src/work_data_hub_pro/apps/orchestration/replay/runtime.py"
      provides: "Shared replay primitives for context creation, checkpoint finalization, and common orchestration steps."
      contains: "execute_replay_run,build_primary_failure,finalize_replay_run"
    - path: "src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py"
      provides: "Explicit annuity runner adopting the shared runtime while keeping domain-owned services and fixtures visible."
      contains: "run_annuity_performance_slice,ReplayRunReport"
    - path: "tests/replay/test_annuity_performance_slice.py"
      provides: "Replay acceptance coverage for the annuity runner after shared-runtime adoption."
      contains: "run_report,primary_failure,comparison_run_id"
  key_links:
    - from: "src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/runtime.py"
      via: "shared runtime primitives only"
      pattern: "execute_replay_run|finalize_replay_run|build_primary_failure"
    - from: "src/work_data_hub_pro/apps/orchestration/replay/runtime.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/errors.py"
      via: "typed setup-failure translation"
      pattern: "translate_replay_setup_error|ReplaySetupError"
---

<objective>
Extract and adopt the shared replay runtime for the annuity slice first, while freezing the PIPE-03 failure behavior under real runner usage.

Purpose: prove the shared-versus-explicit boundary from D-01 through D-04 on the lowest-variance slice before the event-domain runners move.
Output: a shared replay runtime module, annuity slice adoption, and replay/integration coverage proving typed run-report behavior and typed setup failures.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-CONTEXT.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-RESEARCH.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-01-PLAN.md
@.planning/phases/02-transparent-pipeline-contracts-parity-gates/02-02-SUMMARY.md
@.planning/phases/06-phase-2-governance-remediation-truthful-gates-and-status-sync/06-02-SUMMARY.md
@src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py
@src/work_data_hub_pro/governance/compatibility/gate_runtime.py
@tests/replay/test_annuity_performance_slice.py

<interfaces>
Planned contracts from `03-01` that this plan should consume directly:

```python
@dataclass(frozen=True)
class ReplayRunReport:
    comparison_run_id: str
    overall_outcome: str
    checkpoint_results: list[CheckpointResult]
    primary_failure: ReplayPrimaryFailure | None
    compatibility_case: CompatibilityCase | None
    evidence_paths: ReplayEvidencePaths
```

```python
class ReplaySetupError(Exception): ...
def translate_replay_setup_error(domain: str, stage: str, exc: Exception, context: dict[str, object]) -> ReplaySetupError: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared replay primitives and adopt them in the annuity runner per D-01 through D-04</name>
  <files>src/work_data_hub_pro/apps/orchestration/replay/runtime.py, src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py, tests/replay/test_annuity_performance_slice.py</files>
  <read_first>
    .planning/phases/03-orchestration-refactor-failure-explainability/03-CONTEXT.md
    .planning/phases/03-orchestration-refactor-failure-explainability/03-01-PLAN.md
    src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py
    src/work_data_hub_pro/governance/compatibility/gate_runtime.py
    tests/replay/test_annuity_performance_slice.py
  </read_first>
  <action>Create `src/work_data_hub_pro/apps/orchestration/replay/runtime.py` and move only the invariant replay mechanics from D-02 into it: trace and lineage scaffolding, checkpoint construction, gate summarization, evidence package assembly, publication-plan helper usage, and the intake -> processing -> identity loop skeleton. Modify `src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py` to consume those primitives while keeping annuity-specific intake service selection, processor choice, manifest paths, replay asset loading, publication target wiring, projection calls, and payload-shaping helpers explicit in the slice file per D-03. Do not create a generic domain-adapter runner or move annuity business semantics into callbacks/config. Update `tests/replay/test_annuity_performance_slice.py` so passing runs assert the runner still emits the accepted checkpoint list and now exposes a typed `run_report` with `primary_failure is None` on success.</action>
  <acceptance_criteria>`uv run pytest tests/replay/test_annuity_performance_slice.py -v` passes. `rg -n "execute_replay_run|build_primary_failure|finalize_replay_run|ReplayRunReport|run_report" src/work_data_hub_pro/apps/orchestration/replay/runtime.py src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py tests/replay/test_annuity_performance_slice.py` returns hits showing the shared-runtime entrypoints and the annuity runner adoption.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_annuity_performance_slice.py -v</automated>
    <automated>rg -n "execute_replay_run|build_primary_failure|finalize_replay_run|ReplayRunReport|run_report" src/work_data_hub_pro/apps/orchestration/replay/runtime.py src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py tests/replay/test_annuity_performance_slice.py</automated>
  </verify>
  <done>The annuity runner is the first consumer of the shared replay runtime while still reading as an explicit per-domain slice.</done>
</task>

<task type="auto">
  <name>Task 2: Make annuity setup failures typed and keep completed mismatches on the run-report path</name>
  <files>src/work_data_hub_pro/apps/orchestration/replay/runtime.py, src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py, tests/integration/test_replay_setup_failures.py, tests/replay/test_phase2_annuity_performance_gates.py</files>
  <read_first>
    .planning/phases/03-orchestration-refactor-failure-explainability/03-01-PLAN.md
    src/work_data_hub_pro/apps/orchestration/replay/errors.py
    src/work_data_hub_pro/apps/orchestration/replay/runtime.py
    src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py
    tests/integration/test_replay_setup_failures.py
    tests/replay/test_phase2_annuity_performance_gates.py
  </read_first>
  <action>Update the annuity runner and `runtime.py` to wrap baseline loading, manifest loading, publication-policy lookup, and publication-plan validation with `translate_replay_setup_error(...)`. Typed setup failures must still raise, not be converted into fake `CheckpointResult` rows. For completed runs, compute `run_report.primary_failure` from the first failed checkpoint and keep compatibility-case creation on the gate-evaluation path only, as required by D-05 and D-07. Update `tests/integration/test_replay_setup_failures.py` so the annuity runner raises typed setup exceptions for missing baselines and policy/config defects, and update `tests/replay/test_phase2_annuity_performance_gates.py` so failed completed runs assert `run_report.primary_failure.checkpoint_name` matches the first failing checkpoint while `comparison_run_id` and compatibility-case creation still work.</action>
  <acceptance_criteria>`uv run pytest tests/integration/test_replay_setup_failures.py tests/replay/test_phase2_annuity_performance_gates.py -v` passes. `rg -n "translate_replay_setup_error|ReplaySetupError|primary_failure|comparison_run_id|compatibility_case" src/work_data_hub_pro/apps/orchestration/replay/runtime.py src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py tests/integration/test_replay_setup_failures.py tests/replay/test_phase2_annuity_performance_gates.py` returns hits for typed setup failure mapping and primary-failure report logic.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_replay_setup_failures.py tests/replay/test_phase2_annuity_performance_gates.py -v</automated>
    <automated>rg -n "translate_replay_setup_error|ReplaySetupError|primary_failure|comparison_run_id|compatibility_case" src/work_data_hub_pro/apps/orchestration/replay/runtime.py src/work_data_hub_pro/apps/orchestration/replay/annuity_performance_slice.py tests/integration/test_replay_setup_failures.py tests/replay/test_phase2_annuity_performance_gates.py</automated>
  </verify>
  <done>The annuity runner now proves the split failure contract under real replay execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annuity slice -> shared replay runtime | The shared runtime must not absorb annuity-specific business or fixture logic. |
| setup/config failures -> run report | Invalid setup must raise typed exceptions instead of being reported as a governed mismatch. |
| shared runtime -> evidence package output | Report/evidence path generation must stay aligned with the accepted Phase 2 package schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | shared replay runtime boundary | mitigate | Keep only D-02 primitives in `runtime.py`, leave manifest paths, fixture loading, and annuity publication/projection wiring explicit in the slice file, and assert adoption in replay tests. |
| T-03-08 | R | setup-failure reporting | mitigate | Route baseline/config/validator failures through `ReplaySetupError` and verify they raise before checkpoint evaluation starts. |
| T-03-09 | I | run-report evidence linkage | mitigate | Build `primary_failure` and evidence-path metadata from accepted checkpoint/gate outputs and keep package writing on the existing Phase 2 helper path. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/integration/test_replay_setup_failures.py tests/replay/test_annuity_performance_slice.py tests/replay/test_phase2_annuity_performance_gates.py -v`
</verification>

<success_criteria>
- The annuity slice consumes shared replay primitives without becoming a generic adapter-driven runner.
- Typed setup failures and typed completed-run reports both execute through the annuity slice.
- Passing and failing annuity replay tests remain green after the refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/03-orchestration-refactor-failure-explainability/03-03-SUMMARY.md`
</output>
