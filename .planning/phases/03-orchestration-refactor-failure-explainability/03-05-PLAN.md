---
phase: 03-orchestration-refactor-failure-explainability
plan: 05
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - src/work_data_hub_pro/apps/etl_cli/main.py
  - tests/contracts/test_replay_cli_contracts.py
  - tests/contracts/test_replay_diagnose_contracts.py
  - docs/runbooks/annuity-performance-replay.md
  - docs/runbooks/annual-award-replay.md
  - docs/runbooks/annual-loss-replay.md
autonomous: true
requirements:
  - OPS-01
  - PIPE-03
must_haves:
  truths:
    - "Agents can run replay through `replay run`, inspect evidence through `replay diagnose`, and discover supported domains through `replay list-domains`."
    - "Human wrapper commands remain available for the three accepted domains."
    - "The agent-facing CLI output exposes the minimum `comparison_run_id`, `overall_outcome`, `primary_failed_checkpoint`, `evidence_root`, and `compatibility_case_id` contract."
    - "Completed passed, warning, and failed replay runs stay diagnosable by `comparison_run_id` through the same CLI surface."
  artifacts:
    - path: "src/work_data_hub_pro/apps/etl_cli/main.py"
      provides: "Nested `replay` Typer command group plus preserved human wrapper commands."
      contains: "replay run,replay diagnose,replay list-domains,replay-annuity-performance,replay-annual-award,replay-annual-loss"
    - path: "tests/contracts/test_replay_cli_contracts.py"
      provides: "CLI contract coverage for replay discovery and run entrypoints."
      contains: "test_replay_list_domains_contract,test_replay_run_outputs_machine_readable_summary"
    - path: "docs/runbooks/annual-loss-replay.md"
      provides: "Updated human wrapper and agent CLI guidance with temp-id salt prerequisite."
      contains: "replay diagnose,WDHP_TEMP_ID_SALT"
  key_links:
    - from: "src/work_data_hub_pro/apps/etl_cli/main.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/registry.py"
      via: "registry-backed dispatch and repo-root replay paths"
      pattern: "REPLAY_DOMAINS|wrapper_command|replay_root"
    - from: "src/work_data_hub_pro/apps/etl_cli/main.py"
      to: "src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py"
      via: "typed replay diagnose command"
      pattern: "load_replay_diagnostics|ReplayDiagnosticsNotFoundError"
---

<objective>
Finish the phase by exposing stable documented replay entrypoints for both humans and agents.

Purpose: close OPS-01 after the replay contracts, temp-id policy, and shared runtime are already in place.
Output: nested replay CLI commands, preserved wrapper commands, CLI contract tests, and runbooks updated to match the new stable surface.
</objective>

<execution_context>
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/workflows/execute-plan.md
@E:/Projects/WorkDataHubPro/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-CONTEXT.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-RESEARCH.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-01-PLAN.md
@.planning/phases/03-orchestration-refactor-failure-explainability/03-02-PLAN.md
@src/work_data_hub_pro/apps/etl_cli/main.py
@src/work_data_hub_pro/apps/orchestration/replay/registry.py
@src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py
@docs/runbooks/annuity-performance-replay.md
@docs/runbooks/annual-award-replay.md
@docs/runbooks/annual-loss-replay.md

<interfaces>
Registry metadata planned in `03-01` should let the CLI surface these exact values:
- `domain`
- `wrapper_command`
- `replay_root`
- `runbook_path`
- `release_path`
- `domain_config_path`

Run-report summary planned in `03-01` and adopted in `03-03` / `03-04` should let the CLI emit:
- `comparison_run_id`
- `overall_outcome`
- `primary_failed_checkpoint`
- `evidence_root`
- `compatibility_case_id` (`null` when no compatibility case exists for the completed run)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the nested `replay` command group while preserving the existing human wrapper commands</name>
  <files>src/work_data_hub_pro/apps/etl_cli/main.py, tests/contracts/test_replay_cli_contracts.py</files>
  <read_first>
    .planning/phases/03-orchestration-refactor-failure-explainability/03-CONTEXT.md
    src/work_data_hub_pro/apps/etl_cli/main.py
    src/work_data_hub_pro/apps/orchestration/replay/registry.py
    tests/contracts/test_replay_run_report.py
  </read_first>
  <action>Update `src/work_data_hub_pro/apps/etl_cli/main.py` to keep `replay-annuity-performance`, `replay-annual-award`, and `replay-annual-loss` exactly as user-facing wrappers per D-08, but add a nested `replay` Typer app with `run` and `list-domains` subcommands. `replay list-domains` must emit a machine-readable JSON object or array describing each registry entry with `domain`, `wrapper_command`, `replay_root`, `runbook_path`, `release_path`, and `domain_config_path`. `replay run --domain <domain> --workbook <path> --period <period>` must dispatch through the frozen registry runner reference from `REPLAY_DOMAINS`, use repo-root-anchored replay roots by default instead of CWD-relative paths, and print one machine-readable JSON object containing at least `comparison_run_id`, `overall_outcome`, `primary_failed_checkpoint`, `evidence_root`, and `compatibility_case_id`, with `compatibility_case_id: null` when no case exists and optional publication/projection counts appended as extra keys. Add `tests/contracts/test_replay_cli_contracts.py` using `typer.testing.CliRunner` to assert wrapper commands still exist, `replay list-domains` is discoverable, and `replay run` returns the minimum D-10 output keys for a completed run without scraping ad hoc text.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_replay_cli_contracts.py -v` passes. `rg -n "add_typer|replay run|replay list-domains|replay-annuity-performance|replay-annual-award|replay-annual-loss|comparison_run_id|overall_outcome|primary_failed_checkpoint|evidence_root|compatibility_case_id" src/work_data_hub_pro/apps/etl_cli/main.py tests/contracts/test_replay_cli_contracts.py` returns hits for the nested replay app, preserved wrappers, and minimum agent-output keys.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_replay_cli_contracts.py -v</automated>
    <automated>rg -n "add_typer|replay run|replay list-domains|replay-annuity-performance|replay-annual-award|replay-annual-loss|comparison_run_id|overall_outcome|primary_failed_checkpoint|evidence_root|compatibility_case_id" src/work_data_hub_pro/apps/etl_cli/main.py tests/contracts/test_replay_cli_contracts.py</automated>
  </verify>
  <done>The CLI exposes both the preserved human wrappers and the new registry-backed agent run/discovery surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add `replay diagnose` with typed missing-run behavior and machine-readable output</name>
  <files>src/work_data_hub_pro/apps/etl_cli/main.py, tests/contracts/test_replay_diagnose_contracts.py</files>
  <read_first>
    .planning/phases/03-orchestration-refactor-failure-explainability/03-01-PLAN.md
    src/work_data_hub_pro/apps/etl_cli/main.py
    src/work_data_hub_pro/apps/orchestration/replay/diagnostics.py
    tests/contracts/test_replay_diagnose_contracts.py
  </read_first>
  <action>Add `replay diagnose --comparison-run-id <id>` to `src/work_data_hub_pro/apps/etl_cli/main.py` and implement it exclusively through `load_replay_diagnostics(...)`. On success, print a machine-readable JSON object containing the minimum D-10 fields plus `checkpoint_statuses` and `package_paths`, and preserve `compatibility_case_id: null` for completed runs without an adjudication case. On missing run, catch `ReplayDiagnosticsNotFoundError`, print a short typed error message without a Python traceback, and exit non-zero. Extend `tests/contracts/test_replay_diagnose_contracts.py` so it covers both the reader contract from Plan 03-01 and the CLI command behavior through `CliRunner`; use tmp-path package fixtures and registry overrides so the test does not depend on committed evidence roots or shell CWD, and include at least one completed passed-or-warning fixture plus one missing-run fixture.</action>
  <acceptance_criteria>`uv run pytest tests/contracts/test_replay_diagnose_contracts.py -v` passes. `rg -n "replay diagnose|load_replay_diagnostics|ReplayDiagnosticsNotFoundError|checkpoint_statuses|package_paths|comparison_run_id|overall_outcome" src/work_data_hub_pro/apps/etl_cli/main.py tests/contracts/test_replay_diagnose_contracts.py` returns hits for the command, typed error path, and machine-readable diagnose fields.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/contracts/test_replay_diagnose_contracts.py -v</automated>
    <automated>rg -n "replay diagnose|load_replay_diagnostics|ReplayDiagnosticsNotFoundError|checkpoint_statuses|package_paths|comparison_run_id|overall_outcome" src/work_data_hub_pro/apps/etl_cli/main.py tests/contracts/test_replay_diagnose_contracts.py</automated>
  </verify>
  <done>The CLI exposes typed diagnostics lookup by `comparison_run_id` with stable machine-readable output and missing-run behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Update the three replay runbooks to match the preserved wrappers and the new agent CLI surface</name>
  <files>docs/runbooks/annuity-performance-replay.md, docs/runbooks/annual-award-replay.md, docs/runbooks/annual-loss-replay.md</files>
  <read_first>
    .planning/phases/03-orchestration-refactor-failure-explainability/03-CONTEXT.md
    docs/runbooks/annuity-performance-replay.md
    docs/runbooks/annual-award-replay.md
    docs/runbooks/annual-loss-replay.md
    src/work_data_hub_pro/apps/etl_cli/main.py
    config/releases/temp_identity_policy.json
  </read_first>
  <action>Update all three runbooks so the current wrapper command remains the first human-facing example, then add one short `replay run --domain <domain> --workbook <path> --period <period>` example and one `replay diagnose --comparison-run-id <id>` example. Mention `replay list-domains` as the discovery command for agents, document that default replay roots come from the registry rather than the caller's CWD, and add the `WDHP_TEMP_ID_SALT` prerequisite from Plan 03-02 where fallback identity resolution may occur. Do not add deferred surfaces such as `--all-domains`, `etl`, `operator`, or `adjudication` command trees.</action>
  <acceptance_criteria>`rg -n "replay-annuity-performance|replay-annual-award|replay-annual-loss|replay run --domain|replay diagnose --comparison-run-id|replay list-domains|WDHP_TEMP_ID_SALT" docs/runbooks/annuity-performance-replay.md docs/runbooks/annual-award-replay.md docs/runbooks/annual-loss-replay.md` returns hits in all three runbooks. The files still keep the wrapper command as the first execution example for each domain.</acceptance_criteria>
  <verify>
    <automated>rg -n "replay-annuity-performance|replay-annual-award|replay-annual-loss|replay run --domain|replay diagnose --comparison-run-id|replay list-domains|WDHP_TEMP_ID_SALT" docs/runbooks/annuity-performance-replay.md docs/runbooks/annual-award-replay.md docs/runbooks/annual-loss-replay.md</automated>
  </verify>
  <done>The runbooks document the preserved wrapper commands and the new agent-facing replay CLI without drifting into deferred command surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments -> replay domain dispatch | Untrusted domain and comparison-run inputs must resolve only through the governed registry and diagnostics readers. |
| CLI output -> agent automation | Machine-readable fields must stay stable and complete enough that agents do not scrape ad hoc text. |
| runbooks -> operator execution | Docs must reflect the real command and env-var contracts or humans will execute stale entrypoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | T | replay CLI domain dispatch | mitigate | Drive `replay run` and `replay list-domains` from `REPLAY_DOMAINS`, reject unknown domains, and use registry-provided replay roots instead of relative CWD defaults. |
| T-03-14 | I | agent-facing replay output | mitigate | Emit a single machine-readable summary with the exact D-10 fields and cover it in `tests/contracts/test_replay_cli_contracts.py` and `tests/contracts/test_replay_diagnose_contracts.py`. |
| T-03-15 | R | runbook/operator guidance | mitigate | Update each domain runbook to keep the wrapper command, add the new replay CLI examples, and document the `WDHP_TEMP_ID_SALT` prerequisite in the same phase. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/contracts/test_replay_cli_contracts.py tests/contracts/test_replay_diagnose_contracts.py -v`
- `rg -n "replay run --domain|replay diagnose --comparison-run-id|replay list-domains|WDHP_TEMP_ID_SALT" docs/runbooks/annuity-performance-replay.md docs/runbooks/annual-award-replay.md docs/runbooks/annual-loss-replay.md`
</verification>

<success_criteria>
- The nested replay CLI surface exists and is contract-tested.
- The three human wrapper commands remain available.
- Runbooks and CLI output contracts match the implemented entrypoints and temp-id prerequisite.
</success_criteria>

<output>
After completion, create `.planning/phases/03-orchestration-refactor-failure-explainability/03-05-SUMMARY.md`
</output>
