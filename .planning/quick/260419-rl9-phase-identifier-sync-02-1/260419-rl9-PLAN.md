---
quick_task: 260419-rl9
type: quick-validate
description: "将 .planning/ 下所有涉及 legacy remediation 标识的计划文档、phase 标识、路径引用和 STATE.md 记录，统一调整为 02.1-phase-2-governance-remediation-truthful-gates-and-status-sync，并在完成后检查 .planning/ 内不再残留旧标识；如果对应 phase 目录名也使用旧标识，则一并重命名并同步修正所有交叉引用。"
must_haves:
  truths:
    - "`.planning/` 中不再残留 legacy remediation 标识作为该 remediation phase 的标识。"
    - "该 phase 的目录名、文件名前缀、frontmatter `phase:` 值、交叉引用路径和相关状态文案统一改为 `02.1` 体系。"
    - "更新范围保持在 `.planning/`，不改动仓库外部代码或测试文件名。"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Remediation phase section renamed to Phase 02.1 with updated plan identifiers."
      contains: "### Phase 02.1: Phase 2 governance remediation - truthful gates and status sync"
    - path: ".planning/STATE.md"
      provides: "Session history and quick-task tracking updated to reference Phase 02.1."
      contains: "Phase 02.1"
    - path: ".planning/phases/02.1-phase-2-governance-remediation-truthful-gates-and-status-sync/02.1-VERIFICATION.md"
      provides: "Renamed remediation verification artifact under the new phase identifier."
      contains: "phase: 02.1-phase-2-governance-remediation-truthful-gates-and-status-sync"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/02.1-phase-2-governance-remediation-truthful-gates-and-status-sync/02.1-CONTEXT.md"
      via: "renamed remediation phase section and path references"
    - from: ".planning/STATE.md"
      to: ".planning/phases/02.1-phase-2-governance-remediation-truthful-gates-and-status-sync/02.1-CONTEXT.md"
      via: "session note for captured remediation context"
---

# Quick Task 260419-rl9 Plan

1. Inventory and rename the remediation phase directory plus in-phase artifact filenames from `06-*` to `02.1-*`.
   verify: `Get-ChildItem .planning/phases/02.1-phase-2-governance-remediation-truthful-gates-and-status-sync`
   done: The renamed phase directory exists and the old `06-*` artifact names are gone.

2. Update `.planning/` documents so phase titles, `phase:` metadata, plan identifiers, references, and status notes all point at the new `02.1` phase identity.
   verify: Run targeted `rg` scans for the legacy remediation slug, title, plan ids, and command references across `.planning/`.
   done: All matching `.planning/` references for this remediation use `02.1`.

3. Run residual checks, capture the quick-task summary, and write a verification artifact with the exact commands and results.
   verify: Re-run the targeted legacy remediation residual scans and confirm they return no matches.
   done: The residual scan is clean and the quick-task artifacts document the outcome.
