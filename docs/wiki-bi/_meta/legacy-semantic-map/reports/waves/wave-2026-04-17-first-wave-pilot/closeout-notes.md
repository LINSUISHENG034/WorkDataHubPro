# First-wave pilot Phase B closeout note

- Phase B cleared the two closeout blockers by:
  - accepting the active wave's durable wiki targets
  - normalizing the three remaining candidates to `deferred`
- Durable review outcome:
  - `annual_award`, `annual_loss`, and `annuity_performance` required no durable edits
  - `annuity_income` required only a targeted evidence-page reconciliation so branch-mapping placement is no longer described as an open gap
- Deferred candidate dispositions:
  - `cand-company-lookup-queue-publication-boundary`
  - `cand-customer-mdm-manual-runtime-boundary`
  - `cand-reference-sync-governance-bridge`
- `archive_ready` may still remain `false` after regeneration because `wave-2026-04-17-first-wave-pilot` is intentionally left `status: active` in `waves/index.yaml`; archive/closure status is a follow-on decision outside this blocker-clearing Phase B slice.
